import{_ as a,H as n,f as i,i as e}from"./chunks/framework.BHvCMIhP.js";const g=JSON.parse('{"title":"堆漏洞:use-after-free / double free / heap spraying","description":"","frontmatter":{},"headers":[],"relativePath":"securityLearning/18-堆漏洞.md","filePath":"securityLearning/18-堆漏洞.md","lastUpdated":1778496697000}'),t={name:"securityLearning/18-堆漏洞.md"};function p(l,s,o,r,h,c){return n(),i("div",null,[...s[0]||(s[0]=[e(`<h1 id="堆漏洞-use-after-free-double-free-heap-spraying" tabindex="-1">堆漏洞:use-after-free / double free / heap spraying <a class="header-anchor" href="#堆漏洞-use-after-free-double-free-heap-spraying" aria-label="Permalink to &quot;堆漏洞:use-after-free / double free / heap spraying&quot;">​</a></h1><p>讲堆漏洞之前先把一个误区杀掉:「栈漏洞我懂了,堆不就是 <code>malloc/free</code> 嘛,无非把栈帧换成堆块」——<strong>完全不是</strong>。栈漏洞是「一条线性带子被踩穿」,堆漏洞是「一张到处是指针的网被剪断」。17 篇讲了栈溢出 / ROP——栈的布局可预测、Canary 一加就基本封住返回地址改写;<strong>而堆从 2010 年代开始就成了浏览器 / 内核 0day 的主战场</strong>,Chrome / Safari / iOS 每年的 0day 报告里 70-80% 是 UAF。osLearning/08-09 讲过 <code>malloc</code> / glibc 分配器的内部结构,<strong>这一篇换攻击者视角看同样的数据结构</strong>——为什么 tcache 是天堂、为什么 free 一个指针没把它置空是 21 世纪最贵的 NULL、为什么&quot;加一道虚函数表&quot;反而开了一扇大门。</p><blockquote><p>一句话先记住:<strong>栈漏洞是改控制流,堆漏洞是改&quot;数据里的控制流&quot;</strong>——堆上躺着的全是指针:vtable、函数指针、free chunk 的 fd/bk 链表指针、SMART POINTER 的 vtable、JS 引擎对象的 Map 指针。<strong>攻击者不需要改返回地址,只要让你的程序&quot;调用一个你以为是合法对象、但其实是我控制的内存&quot;</strong>,控制流就到手了。<strong>堆漏洞的核心攻击姿势就一句话:让攻击者数据出现在防御者指针解引用的位置</strong>——UAF、Double Free、堆溢出全是这一句的变体。</p></blockquote><hr><h2 id="一、堆-vs-栈-攻击面为什么完全不同" tabindex="-1">一、堆 vs 栈:攻击面为什么完全不同 <a class="header-anchor" href="#一、堆-vs-栈-攻击面为什么完全不同" aria-label="Permalink to &quot;一、堆 vs 栈:攻击面为什么完全不同&quot;">​</a></h2><h3 id="_1-1-布局可预测性-堆是攻击者的劣势" tabindex="-1">1.1 布局可预测性:堆是攻击者的劣势 <a class="header-anchor" href="#_1-1-布局可预测性-堆是攻击者的劣势" aria-label="Permalink to &quot;1.1 布局可预测性:堆是攻击者的劣势&quot;">​</a></h3><p>栈的布局是<strong>确定的</strong>——同一个二进制、同一段代码,函数 A 调用函数 B,B 的栈帧永远在 A 下面 32 字节;Canary、保存的 rbp、返回地址,<strong>位置稳如老狗</strong>。栈溢出的利用脚本可以写死偏移量,跑十次成功十次。</p><p>堆完全相反:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>malloc(100) 返回什么地址?</span></span>
<span class="line"><span>   依赖之前 N 次 malloc/free 的历史</span></span>
<span class="line"><span>   依赖 glibc 版本(2.27 / 2.31 / 2.34 行为不同)</span></span>
<span class="line"><span>   依赖 ASLR(每次进程启动堆基址都不一样)</span></span>
<span class="line"><span>   依赖线程(每个线程有自己的 arena)</span></span>
<span class="line"><span>   依赖 size(&lt;= 64B 进 tcache,大块进 mmap)</span></span></code></pre></div><p><strong>所以堆漏洞利用的第一步不是&quot;打&quot;,是&quot;整理&quot;</strong>——攻击者要先把堆&quot;按摩&quot;(heap massaging / heap feng shui)到一个可预测的形态,才能开始真正的攻击。这是一项手艺活,也是堆 exploit 比栈 exploit 难一个数量级的原因。</p><h3 id="_1-2-攻击面广度-堆是攻击者的优势" tabindex="-1">1.2 攻击面广度:堆是攻击者的优势 <a class="header-anchor" href="#_1-2-攻击面广度-堆是攻击者的优势" aria-label="Permalink to &quot;1.2 攻击面广度:堆是攻击者的优势&quot;">​</a></h3><p>栈上能改的就那几样:局部变量、保存寄存器、返回地址。<strong>堆上躺着的全是指针</strong>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>堆里随便摸一把,大概率摸到:</span></span>
<span class="line"><span>   ├── C++ 对象的 vtable 指针(改了 → 接管所有虚函数调用)</span></span>
<span class="line"><span>   ├── 回调函数指针(改了 → 接管某个事件)</span></span>
<span class="line"><span>   ├── 字符串 / buffer 的指向(改了 → 任意读写原语)</span></span>
<span class="line"><span>   ├── 链表 / 树的 next/prev(改了 → 任意地址写)</span></span>
<span class="line"><span>   ├── glibc free chunk 的 fd/bk(改了 → unlink / tcache 攻击)</span></span>
<span class="line"><span>   ├── JS 引擎对象的 Map / Shape 指针(改了 → 类型混淆)</span></span>
<span class="line"><span>   └── refcount(改了 → 提前 free → UAF)</span></span></code></pre></div><p><strong>栈是一根线,堆是一张网</strong>——攻击面不是线性而是组合爆炸。也因此,堆漏洞的利用形态远比栈丰富,缓解机制(Canary / ASLR / DEP)对栈很有效,<strong>对堆要打的折扣大得多</strong>。</p><h3 id="_1-3-时间维度-堆有-生命周期" tabindex="-1">1.3 时间维度:堆有&quot;生命周期&quot; <a class="header-anchor" href="#_1-3-时间维度-堆有-生命周期" aria-label="Permalink to &quot;1.3 时间维度:堆有&quot;生命周期&quot;&quot;">​</a></h3><p>栈帧的生命周期等于函数调用——退出即销毁,<strong>没有&quot;用完之后还能再访问&quot;的概念</strong>。</p><p>堆对象的生命周期是<strong>程序员管理</strong>的——<code>malloc</code> 之后什么时候 <code>free</code>、free 之后那块内存被谁拿走、原来指向它的指针有没有置空,<strong>全靠开发者自觉</strong>。一旦自觉不到位,UAF 就在那儿等着。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>栈漏洞的本质:空间错误(写到不该写的地方)</span></span>
<span class="line"><span>堆漏洞的本质:空间错误 + 时间错误</span></span>
<span class="line"><span>   ├── 空间:堆溢出(写到相邻 chunk)</span></span>
<span class="line"><span>   └── 时间:UAF / Double Free(在错误的时刻访问)</span></span></code></pre></div><p><strong>时间漏洞是堆独有的</strong>——这是为什么 Rust 的 ownership 模型对堆漏洞特别管用而对栈漏洞用处一般。</p><hr><h2 id="二、glibc-ptmalloc-心智-理解攻击面所需的最小模型" tabindex="-1">二、glibc ptmalloc 心智:理解攻击面所需的最小模型 <a class="header-anchor" href="#二、glibc-ptmalloc-心智-理解攻击面所需的最小模型" aria-label="Permalink to &quot;二、glibc ptmalloc 心智:理解攻击面所需的最小模型&quot;">​</a></h2><p>不重复 osLearning/09 已经讲过的细节,<strong>这里只补攻击者必须知道的三件事</strong>。</p><h3 id="_2-1-chunk-的结构" tabindex="-1">2.1 chunk 的结构 <a class="header-anchor" href="#_2-1-chunk-的结构" aria-label="Permalink to &quot;2.1 chunk 的结构&quot;">​</a></h3><p>glibc 每一块堆内存(无论 in-use 还是 free)都长这样:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>┌─────────────────────────┐  &lt;- chunk 起始</span></span>
<span class="line"><span>│ prev_size               │   8 bytes(只在前一个 chunk 是 free 时有意义)</span></span>
<span class="line"><span>├─────────────────────────┤</span></span>
<span class="line"><span>│ size | A | M | P        │   8 bytes(size + 3 个标志位)</span></span>
<span class="line"><span>├─────────────────────────┤  &lt;- malloc 返回的指针指这里</span></span>
<span class="line"><span>│                         │</span></span>
<span class="line"><span>│  用户数据 (in-use)       │   或</span></span>
<span class="line"><span>│  fd/bk 指针 (free)      │</span></span>
<span class="line"><span>│                         │</span></span>
<span class="line"><span>└─────────────────────────┘</span></span>
<span class="line"><span>              ↑</span></span>
<span class="line"><span>   下一个 chunk 的 prev_size 紧贴着</span></span></code></pre></div><p><strong>关键观察</strong>:<strong>chunk 的元数据(size、prev_size、fd/bk)和用户数据是连续的</strong>——上一个 chunk 的用户数据写溢出一个字节,就踩到下一个 chunk 的 prev_size;溢出更多,就踩到 size、踩到 fd 指针。<strong>这就是堆溢出能改控制流的物理基础</strong>。</p><h3 id="_2-2-三个-bin-tcache-fastbin-unsorted-bin" tabindex="-1">2.2 三个 bin:tcache / fastbin / unsorted bin <a class="header-anchor" href="#_2-2-三个-bin-tcache-fastbin-unsorted-bin" aria-label="Permalink to &quot;2.2 三个 bin:tcache / fastbin / unsorted bin&quot;">​</a></h3><p>free 掉的 chunk 不会立刻还给 OS,而是按 size 分类挂到几个链表(bin)上,等下次 <code>malloc</code> 同样大小直接复用:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>   tcache(每线程,无锁,最快)</span></span>
<span class="line"><span>      ├── 0x20  : [chunk_a] -&gt; [chunk_b] -&gt; ...</span></span>
<span class="line"><span>      ├── 0x30  : [chunk_c] -&gt; ...</span></span>
<span class="line"><span>      └── ... 64 个大小档,每档最多 7 个</span></span>
<span class="line"><span></span></span>
<span class="line"><span>   fastbin(全局,size &lt;= 0x80,单链表)</span></span>
<span class="line"><span>      ├── 0x20  : [chunk_d] -&gt; ...</span></span>
<span class="line"><span>      └── ...</span></span>
<span class="line"><span></span></span>
<span class="line"><span>   unsorted bin(其他 size,双链表)</span></span>
<span class="line"><span>      └── [chunk_e] &lt;-&gt; [chunk_f] &lt;-&gt; ...</span></span></code></pre></div><p><strong>攻击者要关心的就一件事</strong>:<strong>free chunk 的 fd / bk 指针就在用户数据区</strong>——也就是说,<strong>当一块内存从 in-use 变成 free,前 8/16 字节就从&quot;用户数据&quot;变成了&quot;指向下一个 free chunk 的指针&quot;</strong>;反过来,<strong>当 malloc 拿走这块内存,前 8 字节又变回&quot;用户数据&quot;</strong>,但<strong>内容是上次 free 时写下的 fd 指针</strong>。</p><p>这就给了攻击者一个核心姿势:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>   原本是用户数据 → free → 现在是 fd 指针</span></span>
<span class="line"><span>                          ↑</span></span>
<span class="line"><span>                  攻击者如果能改这 8 字节</span></span>
<span class="line"><span>                  (比如通过 UAF / 堆溢出写邻居)</span></span>
<span class="line"><span>                  下一次 malloc 同 size 会返回攻击者指定的地址</span></span></code></pre></div><p><strong>这就是 tcache poisoning / fastbin attack 的本质</strong>——一句话:<strong>把 free 链表的 next 指针改成&quot;任意地址&quot;,分配器会乖乖把那个地址当成 chunk 还给你</strong>。</p><h3 id="_2-3-safe-linking-glibc-2-32-的缓解" tabindex="-1">2.3 Safe-Linking:glibc 2.32+ 的缓解 <a class="header-anchor" href="#_2-3-safe-linking-glibc-2-32-的缓解" aria-label="Permalink to &quot;2.3 Safe-Linking:glibc 2.32+ 的缓解&quot;">​</a></h3><p>2020 年 glibc 加了 Safe-Linking——free chunk 的 fd 指针不再明文存,而是和 chunk 自身地址的高位异或一下再存:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>存:  fd_in_memory = (real_fd) XOR (chunk_addr &gt;&gt; 12)</span></span>
<span class="line"><span>取:  real_fd      = fd_in_memory XOR (chunk_addr &gt;&gt; 12)</span></span></code></pre></div><p><strong>攻击者要伪造一个&quot;指向任意地址&quot;的 fd,就必须知道 chunk 自身地址</strong>——也就是说,<strong>必须先有一个堆地址泄露</strong>。把&quot;无信息泄露直接 EXP&quot;的难度从 1 提升到了 2,但<strong>对有信息泄露原语的攻击者基本没影响</strong>。</p><hr><h2 id="三、use-after-free-uaf-浏览器-0day-第一大类" tabindex="-1">三、Use-After-Free(UAF):浏览器 0day 第一大类 <a class="header-anchor" href="#三、use-after-free-uaf-浏览器-0day-第一大类" aria-label="Permalink to &quot;三、Use-After-Free(UAF):浏览器 0day 第一大类&quot;">​</a></h2><h3 id="_3-1-漏洞机制" tabindex="-1">3.1 漏洞机制 <a class="header-anchor" href="#_3-1-漏洞机制" aria-label="Permalink to &quot;3.1 漏洞机制&quot;">​</a></h3><p>UAF 的最小模型只有三步:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. 分配:  p = malloc(sizeof(Object));</span></span>
<span class="line"><span>2. 释放:  free(p);</span></span>
<span class="line"><span>          // ←  没有 p = NULL,p 还指向那块内存(dangling pointer)</span></span>
<span class="line"><span>3. 误用:  p-&gt;some_function();   // ← 使用已被释放的对象</span></span></code></pre></div><p><strong>单看这三行没问题——问题在第 2 步和第 3 步之间</strong>:如果攻击者能让 <code>malloc</code> 在两步之间分配回那块内存(re-allocate 同样的 size),<strong>那块内存就被攻击者数据占据</strong>;接下来第 3 步访问 <code>p-&gt;some_function()</code> 时,读到的是攻击者数据。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    时刻 T1:   p ───→ ┌─────────────────┐</span></span>
<span class="line"><span>                      │ vtable_ptr      │ ─── 指向合法 vtable</span></span>
<span class="line"><span>                      │ field_1         │</span></span>
<span class="line"><span>                      │ field_2         │</span></span>
<span class="line"><span>                      └─────────────────┘</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    时刻 T2:   free(p);   // p 还在,但内存进了 tcache</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    时刻 T3:   攻击者触发 malloc 同 size,这块被分配出去</span></span>
<span class="line"><span>               q = malloc(same_size);</span></span>
<span class="line"><span>               memcpy(q, attacker_data, same_size);</span></span>
<span class="line"><span></span></span>
<span class="line"><span>               p ───→ ┌─────────────────┐</span></span>
<span class="line"><span>                      │ FAKE_vtable_ptr │ ←── 攻击者控制!</span></span>
<span class="line"><span>                      │ ...             │</span></span>
<span class="line"><span>                      └─────────────────┘</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    时刻 T4:   p-&gt;virtual_call();</span></span>
<span class="line"><span>               // 读 p[0] 拿 vtable,跳到 vtable[offset]</span></span>
<span class="line"><span>               // → 跳到攻击者指定地址</span></span></code></pre></div><h3 id="_3-2-最小-poc-c" tabindex="-1">3.2 最小 PoC(C++) <a class="header-anchor" href="#_3-2-最小-poc-c" aria-label="Permalink to &quot;3.2 最小 PoC(C++)&quot;">​</a></h3><div class="language-c vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">class Obj {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">public:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    virtual </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> greet</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">printf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;hello</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">); }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> main</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Obj</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> p </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> new </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Obj</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    delete p;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                          // p 悬空但没置 NULL</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    long*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> fake </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> new </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">];</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          // 攻击者重新占同一块</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    fake</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)system;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            // 伪造 &quot;vtable&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    p-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">greet</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                        // 跳到 system!</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><strong>这 10 行揭示了 UAF 的全部本质</strong>——<code>delete p</code> 之后 <code>p</code> 仍然有效,<code>new long[2]</code> 大概率拿到同一块内存(glibc tcache LIFO),<code>p-&gt;greet()</code> 解引用第一个 8 字节当虚表,直接被改写。</p><h3 id="_3-3-真实武器化的两个难点" tabindex="-1">3.3 真实武器化的两个难点 <a class="header-anchor" href="#_3-3-真实武器化的两个难点" aria-label="Permalink to &quot;3.3 真实武器化的两个难点&quot;">​</a></h3><p>教学 PoC 干净,<strong>真实利用要解决两个问题</strong>:</p><p><strong>问题 1:精确占位</strong>。<code>free(p)</code> 之后,要保证下一个<strong>攻击者控制大小</strong>的 <code>malloc</code> 拿到同一块内存。手段是「<strong>堆按摩</strong>」:在触发漏洞前先 <code>malloc / free</code> 一堆 chunk,把 tcache 队列调到一个可预测状态。浏览器里就是连续 <code>new ArrayBuffer / new String</code> 制造特定大小的堆喷射。</p><p><strong>问题 2:vtable 跳板</strong>。即使能伪造 vtable 指针,<strong>还需要 vtable 里有一个能跳到 shellcode 的函数指针</strong>——而 shellcode 还要解决 DEP(数据页不可执行)。这就和栈漏洞合流了——<strong>改 vtable 触发 ROP 链</strong>,17 篇讲过。</p><hr><h2 id="四、double-free-bin-链表的逻辑炸药" tabindex="-1">四、Double Free:bin 链表的逻辑炸药 <a class="header-anchor" href="#四、double-free-bin-链表的逻辑炸药" aria-label="Permalink to &quot;四、Double Free:bin 链表的逻辑炸药&quot;">​</a></h2><h3 id="_4-1-漏洞机制" tabindex="-1">4.1 漏洞机制 <a class="header-anchor" href="#_4-1-漏洞机制" aria-label="Permalink to &quot;4.1 漏洞机制&quot;">​</a></h3><p>同一个指针被 <code>free</code> 两次:</p><div class="language-c vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">free</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(p);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">free</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(p);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   // ← p 已经在 tcache 链表里了,再 push 一次</span></span></code></pre></div><p><strong>glibc tcache 在 2.27-2.28 完全没检查这种情况</strong>(到 2.29 才加了 key 字段检测,但还能绕过)。后果:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>   tcache 链表:  head -&gt; [p] -&gt; [p] -&gt; NULL</span></span>
<span class="line"><span>                          ↑       ↑</span></span>
<span class="line"><span>                       同一块内存,被链了两次!</span></span></code></pre></div><p>接下来连续两次 <code>malloc(size)</code> 都会返回同一块 <code>p</code>——<strong>但分配器认为是两块独立的内存</strong>。攻击者:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>   a = malloc(size);   // 拿到 p</span></span>
<span class="line"><span>   b = malloc(size);   // 又拿到 p,但分配器以为是新块</span></span>
<span class="line"><span>   *a = (long)target_addr;</span></span>
<span class="line"><span>   // 此时 b 的&quot;用户数据&quot;也被改了</span></span>
<span class="line"><span>   c = malloc(size);   // 分配器从 tcache 取出 next,</span></span>
<span class="line"><span>                       // next = *a = target_addr!</span></span>
<span class="line"><span>   // c 现在指向 target_addr,任意地址写得到</span></span></code></pre></div><p><strong>核心姿势</strong>:<strong>Double Free 把同一块内存的&quot;用户数据写权限&quot;和&quot;free chunk fd 指针&quot;叠加在一起</strong>——往用户数据里写就是改 fd,把 fd 改成任意地址,下次 <code>malloc</code> 就返回任意地址。</p><h3 id="_4-2-现代缓解-tcache-key" tabindex="-1">4.2 现代缓解:tcache key <a class="header-anchor" href="#_4-2-现代缓解-tcache-key" aria-label="Permalink to &quot;4.2 现代缓解:tcache key&quot;">​</a></h3><p>glibc 2.29 在每个 tcache chunk 里加了一个 <code>key</code> 字段,<code>free</code> 时检查:<strong>如果这块的 key 已经是当前 tcache 的标识,说明它已在链表里,abort()</strong>。</p><div class="language-c vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// glibc 简化逻辑</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (chunk</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">key </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> tcache) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 已经在 tcache 里了,重复 free</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    abort</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">chunk</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">key </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> tcache;</span></span></code></pre></div><p><strong>绕过姿势</strong>(不展开):key 字段就在用户数据区,<strong>如果同时有个堆溢出能写到这个字段,把它清零就绕过检测</strong>。安全研究界把这种&quot;漏洞链组合&quot;叫做 House of XXX(Spirit / Force / Einherjar / ...)系列——每一种都是对特定 glibc 版本的绕过技巧。</p><hr><h2 id="五、堆溢出与-unlink-攻击-经典中的经典" tabindex="-1">五、堆溢出与 unlink 攻击:经典中的经典 <a class="header-anchor" href="#五、堆溢出与-unlink-攻击-经典中的经典" aria-label="Permalink to &quot;五、堆溢出与 unlink 攻击:经典中的经典&quot;">​</a></h2><h3 id="_5-1-堆溢出基本面" tabindex="-1">5.1 堆溢出基本面 <a class="header-anchor" href="#_5-1-堆溢出基本面" aria-label="Permalink to &quot;5.1 堆溢出基本面&quot;">​</a></h3><div class="language-c vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">char*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> p </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> malloc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">strcpy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(p, attacker_controlled);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   // 没限长</span></span></code></pre></div><p>写超过 64 字节,<strong>踩到的不是别的局部变量,是相邻 chunk 的元数据</strong>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>   ┌─────────────────┐</span></span>
<span class="line"><span>   │ p 的 chunk header │</span></span>
<span class="line"><span>   ├─────────────────┤</span></span>
<span class="line"><span>   │ p 的用户数据 (64B)│</span></span>
<span class="line"><span>   │                 │</span></span>
<span class="line"><span>   │   ↓ 溢出方向     │</span></span>
<span class="line"><span>   ├─────────────────┤  &lt;- 邻居 chunk 边界</span></span>
<span class="line"><span>   │ 邻居 prev_size  │</span></span>
<span class="line"><span>   │ 邻居 size       │</span></span>
<span class="line"><span>   │ 邻居 fd          │     ← 改这里就能 tcache poisoning</span></span>
<span class="line"><span>   │ 邻居 bk          │</span></span>
<span class="line"><span>   └─────────────────┘</span></span></code></pre></div><p><strong>如果邻居是 in-use chunk,改它的 size / 用户数据;如果邻居是 free chunk,改它的 fd / bk</strong>——后者直接通向链表攻击。</p><h3 id="_5-2-unlink-2003-年的经典攻击" tabindex="-1">5.2 unlink:2003 年的经典攻击 <a class="header-anchor" href="#_5-2-unlink-2003-年的经典攻击" aria-label="Permalink to &quot;5.2 unlink:2003 年的经典攻击&quot;">​</a></h3><p>unlink 是分配器从双链表删除一个 chunk 的操作:</p><div class="language-c vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 简化的 unlink 宏</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">FD </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> P</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">fd;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">BK </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> P</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">bk;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">FD</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">bk </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> BK;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">BK</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">fd </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> FD;</span></span></code></pre></div><p><strong>注意 <code>FD-&gt;bk = BK</code> 这一步</strong>——它<strong>写</strong>了 <code>FD</code> 指向的地址。如果攻击者控制了 <code>P-&gt;fd</code>(通过堆溢出),就能让 <code>FD = 任意地址</code>,然后 <code>*(任意地址 + offset_of_bk) = BK</code>(BK 也是攻击者控制的)。<strong>等价于一个&quot;任意地址写任意值&quot;原语</strong>。</p><p>2004 年 glibc 加了 unlink 一致性检查:</p><div class="language-c vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (P</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">fd</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">bk </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> P </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">||</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> P</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">bk</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">fd </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> P) </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">abort</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><p>把简单粗暴的 unlink 干死了,但<strong>House of XXX 系列的演化就是从这里开始的</strong>——研究者发现绕过这个检查的方法,新的攻击姿势又冒出来。这是一场持续 20 年的猫鼠游戏。</p><hr><h2 id="六、c-虚表劫持-为什么-oop-是堆漏洞的放大器" tabindex="-1">六、C++ 虚表劫持:为什么 OOP 是堆漏洞的放大器 <a class="header-anchor" href="#六、c-虚表劫持-为什么-oop-是堆漏洞的放大器" aria-label="Permalink to &quot;六、C++ 虚表劫持:为什么 OOP 是堆漏洞的放大器&quot;">​</a></h2><p>虚函数(<code>virtual</code>)的实现机制:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>   C++ 对象在堆上:</span></span>
<span class="line"><span>   ┌───────────────────┐  &lt;- new 返回的指针</span></span>
<span class="line"><span>   │ vtable_ptr        │ ──→  vtable (在 .rodata)</span></span>
<span class="line"><span>   ├───────────────────┤        ┌────────────────┐</span></span>
<span class="line"><span>   │ member_a          │        │ &amp;Foo::method_0 │</span></span>
<span class="line"><span>   │ member_b          │        │ &amp;Foo::method_1 │</span></span>
<span class="line"><span>   └───────────────────┘        │ ...            │</span></span>
<span class="line"><span>                                └────────────────┘</span></span>
<span class="line"><span></span></span>
<span class="line"><span>   obj-&gt;method_1() 编译成:</span></span>
<span class="line"><span>      mov rax, [obj]          ; 取 vtable_ptr</span></span>
<span class="line"><span>      call [rax + 8]          ; 调 vtable[1]</span></span></code></pre></div><p><strong>致命点</strong>:<strong><code>vtable_ptr</code> 就在对象的第一个 8 字节,和用户数据放一起</strong>。任何能写到这块内存的漏洞(UAF / 堆溢出 / type confusion)都能改 vtable。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>   攻击者控制堆里某 8 字节</span></span>
<span class="line"><span>   → 改 vtable_ptr 指向攻击者伪造的 vtable</span></span>
<span class="line"><span>   → 伪造 vtable[1] = system 地址(或 ROP gadget)</span></span>
<span class="line"><span>   → 程序虚函数调用 → 跳到攻击者代码</span></span></code></pre></div><p><strong>整个 C++ / Chrome / JS 引擎都建立在虚函数之上</strong>——Blink 的 DOM 节点、V8 的 HeapObject、libstdc++ 的 stream,<strong>任何一个 UAF 都能用这条路径打穿</strong>。这就是为什么 Chrome 工程师恨堆漏洞恨到要发明 MiraclePtr。</p><h3 id="现代缓解-cfi-control-flow-integrity" tabindex="-1">现代缓解:CFI(Control Flow Integrity) <a class="header-anchor" href="#现代缓解-cfi-control-flow-integrity" aria-label="Permalink to &quot;现代缓解:CFI(Control Flow Integrity)&quot;">​</a></h3><p>Clang/LLVM 的 CFI 在每次虚函数调用前插入检查:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>   call_virtual(obj, idx):</span></span>
<span class="line"><span>      vtable = obj-&gt;vtable_ptr</span></span>
<span class="line"><span>      if (vtable not in valid_vtable_set):</span></span>
<span class="line"><span>         abort()                    ; ← CFI 检查</span></span>
<span class="line"><span>      call vtable[idx]</span></span></code></pre></div><p><strong>把&quot;任意 vtable&quot;收窄到&quot;已知合法的 vtable&quot;</strong>。Chrome / Edge / Android 系统组件都开了 CFI,<strong>vtable 劫持的难度从&quot;找到 UAF&quot;上升到&quot;找到 UAF + 找到 CFI 绕过&quot;</strong>。19 篇还会专门讲 CFI / Shadow Stack。</p><hr><h2 id="七、heap-spraying-浏览器时代的-用空间换确定性" tabindex="-1">七、Heap Spraying:浏览器时代的&quot;用空间换确定性&quot; <a class="header-anchor" href="#七、heap-spraying-浏览器时代的-用空间换确定性" aria-label="Permalink to &quot;七、Heap Spraying:浏览器时代的&quot;用空间换确定性&quot;&quot;">​</a></h2><h3 id="_7-1-思路" tabindex="-1">7.1 思路 <a class="header-anchor" href="#_7-1-思路" aria-label="Permalink to &quot;7.1 思路&quot;">​</a></h3><p>UAF 利用要求&quot;重新分配同位置&quot;,但浏览器堆地址不可预测——<strong>怎么提高命中率?用海量占位</strong>。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>   攻击者在浏览器 JS / Flash 里:</span></span>
<span class="line"><span>   for (i = 0; i &lt; 100000; i++) {</span></span>
<span class="line"><span>       arr[i] = new ArrayBuffer(4096);</span></span>
<span class="line"><span>       fill_with_shellcode_or_gadgets(arr[i]);</span></span>
<span class="line"><span>   }</span></span>
<span class="line"><span></span></span>
<span class="line"><span>   堆里现在的样子:</span></span>
<span class="line"><span>   ┌────────┬────────┬────────┬────────┬────────┐</span></span>
<span class="line"><span>   │payload │payload │payload │payload │payload │ ... 几百 MB</span></span>
<span class="line"><span>   └────────┴────────┴────────┴────────┴────────┘</span></span></code></pre></div><p>跳到任何随机地址,<strong>只要落在这片区域内</strong>,执行的都是攻击者的 payload。</p><p><strong>配上 NOP sled(在 payload 头部填一长串 NOP 指令)</strong>,精确度要求进一步降低——跳到 payload 前任何位置都会一路 NOP 滑到真正的 shellcode。</p><h3 id="_7-2-现代缓解" tabindex="-1">7.2 现代缓解 <a class="header-anchor" href="#_7-2-现代缓解" aria-label="Permalink to &quot;7.2 现代缓解&quot;">​</a></h3><p>DEP / W^X 让&quot;喷一片 shellcode 等执行&quot;基本失效——堆页默认 RW、不可执行。<strong>于是堆喷射演化成&quot;喷 ROP 链 + 喷伪造对象&quot;</strong>——喷的不是可执行代码,而是用来劫持控制流后跳转用的 gadget 地址表。</p><p>JS 引擎层面也有缓解:V8 把大 ArrayBuffer 用 mmap 单独分配(不在主堆里)、限制 typed array 大小、把堆地址做随机化偏移。<strong>老式 spray 在现代浏览器基本绝迹,但思想还在</strong>——任何&quot;我需要让某个未知地址变成已知内容&quot;的攻击都会有 spray 的变体。</p><hr><h2 id="八、浏览器-堆漏洞的核战场" tabindex="-1">八、浏览器:堆漏洞的核战场 <a class="header-anchor" href="#八、浏览器-堆漏洞的核战场" aria-label="Permalink to &quot;八、浏览器:堆漏洞的核战场&quot;">​</a></h2><h3 id="_8-1-为什么浏览器堆漏洞这么多" tabindex="-1">8.1 为什么浏览器堆漏洞这么多 <a class="header-anchor" href="#_8-1-为什么浏览器堆漏洞这么多" aria-label="Permalink to &quot;8.1 为什么浏览器堆漏洞这么多&quot;">​</a></h3><ul><li><strong>JS 暴露给攻击者一个图灵完备的、可触发任意内存分配 / 释放的接口</strong>——<code>new Array(N) / arr.length = M / arr.fill(x)</code>,攻击者可以编程操纵堆。</li><li><strong>DOM / V8 的对象模型大量用虚函数和指针</strong>——任何 UAF 都能升级成 RCE。</li><li><strong>JIT 编译器引入 type confusion</strong>——猜错对象 shape 就解引用错误的字段,等价于 UAF。</li><li><strong>沙箱外的代码量巨大</strong>——Chrome 渲染进程几千万行 C++,UAF 是常驻 bug。</li></ul><p>Google Project Zero 历年统计:<strong>浏览器 / 内核 0day 中 70-80% 是内存安全漏洞,其中堆相关漏洞又占大头</strong>。</p><h3 id="_8-2-chrome-的两个新缓解" tabindex="-1">8.2 Chrome 的两个新缓解 <a class="header-anchor" href="#_8-2-chrome-的两个新缓解" aria-label="Permalink to &quot;8.2 Chrome 的两个新缓解&quot;">​</a></h3><p><strong>MiraclePtr(BackupRefPtr)</strong> —— 2022 年开始全量铺开。原理:把所有 <code>T*</code> 替换成带引用计数的 <code>raw_ptr&lt;T&gt;</code>,<strong>释放时如果还有活引用,推迟释放或者把内存写垃圾值,让后续 UAF 立刻崩溃而不是被利用</strong>。代价:每个指针多 8 字节、解引用多一次内存访问。<strong>实测把 Chrome 渲染进程的 UAF 类 0day 灭掉 50%+</strong>。</p><p><strong>V8 Sandbox</strong> —— 2023 年起在 Chrome 桌面默认开启。原理:把所有 V8 对象指针变成&quot;沙箱内偏移量(32 位 index)&quot;,<strong>即使攻击者拿到 V8 堆内任意读写,也只能在 V8 沙箱内乱跳,跳不出去</strong>。再配合外层进程沙箱,<strong>把&quot;V8 RCE → 操作系统 RCE&quot;这一跳堵死</strong>。</p><h3 id="_8-3-ios-safari-的姿势-pac-mte-路线" tabindex="-1">8.3 iOS / Safari 的姿势:PAC + MTE 路线 <a class="header-anchor" href="#_8-3-ios-safari-的姿势-pac-mte-路线" aria-label="Permalink to &quot;8.3 iOS / Safari 的姿势:PAC + MTE 路线&quot;">​</a></h3><p>苹果 A12 起 CPU 支持 PAC(Pointer Authentication Codes)——指针在高 16 位带签名,跨边界使用前 CPU 自动校验,签错就崩。<strong>对劫持 vtable / 函数指针特别有效</strong>。</p><p>ARMv9 的 MTE(Memory Tagging Extension)更狠——每 16 字节内存分配一个 4-bit tag,指针高位也带 tag,<strong>访问时硬件比较,不匹配就崩</strong>。<strong>这等于在硬件层面把 UAF / 堆溢出做了运行时检查</strong>,开销 &lt; 5%。Pixel 8 / iPhone 16(传)开始硬件支持,<strong>未来 5 年最有希望从根本上杀掉堆漏洞的技术</strong>。</p><h3 id="_8-4-android-fuchsia-的-scudo" tabindex="-1">8.4 Android / Fuchsia 的 scudo <a class="header-anchor" href="#_8-4-android-fuchsia-的-scudo" aria-label="Permalink to &quot;8.4 Android / Fuchsia 的 scudo&quot;">​</a></h3><p>Google 的 scudo 分配器(Android 11+ 默认)替代了 jemalloc / dlmalloc,内置:</p><ul><li>chunk header 自校验(改一个字节就崩)</li><li>随机化 chunk 顺序(削弱 heap massaging)</li><li>delayed free(分配器维护一个 quarantine,释放的 chunk 先冷藏一段时间再复用,UAF 窗口被拉长难度上升)</li><li>guard page(大块分配两边夹不可访问页,溢出立刻崩)</li></ul><p><strong>思路和 hardened malloc / GrapheneOS 的 malloc 一致</strong>——以一定运行时开销换取&quot;漏洞触发就崩,而不是被静默利用&quot;。</p><hr><h2 id="九、真实案例-heartbleed-与近年-0day" tabindex="-1">九、真实案例:Heartbleed 与近年 0day <a class="header-anchor" href="#九、真实案例-heartbleed-与近年-0day" aria-label="Permalink to &quot;九、真实案例:Heartbleed 与近年 0day&quot;">​</a></h2><h3 id="_9-1-heartbleed-cve-2014-0160" tabindex="-1">9.1 Heartbleed(CVE-2014-0160) <a class="header-anchor" href="#_9-1-heartbleed-cve-2014-0160" aria-label="Permalink to &quot;9.1 Heartbleed(CVE-2014-0160)&quot;">​</a></h3><p>不是经典的 UAF / overflow,而是<strong>堆上越界读</strong>——OpenSSL 的 TLS heartbeat 实现里:</p><div class="language-c vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">memcpy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(response, request</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, request</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// request-&gt;length 来自攻击者,但没和实际 data 大小校对</span></span></code></pre></div><p>攻击者发一个 <code>length = 65535</code> 但实际 <code>data</code> 只有 1 字节的请求,服务器从 <code>request-&gt;data</code> 这个堆指针开始<strong>往后读 65535 字节</strong>——读到的全是堆上别的对象残骸:<strong>私钥、session ticket、其他用户的 HTTP 请求、密码</strong>。</p><p><strong>Heartbleed 经典在于揭示了堆漏洞的另一面</strong>——不需要写、只要能读,就可能把整个系统的秘密 dump 出来。<strong>修复 ≠ 加锁、加防御</strong>,<strong>而是验证长度</strong>(<code>if (request-&gt;length &gt; actual_data_size) abort</code>)——本质是 C 没有自动边界检查。</p><h3 id="_9-2-chrome-ios-近年-0day-缩影" tabindex="-1">9.2 Chrome / iOS 近年 0day 缩影 <a class="header-anchor" href="#_9-2-chrome-ios-近年-0day-缩影" aria-label="Permalink to &quot;9.2 Chrome / iOS 近年 0day 缩影&quot;">​</a></h3><p>不列具体 CVE 编号(00 写作计划禁止),只列模式:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>攻击模式 1:V8 的 JIT 类型推断出错</span></span>
<span class="line"><span>  → 把对象误以为另一种 shape</span></span>
<span class="line"><span>  → 字段偏移算错 → 越界读写 V8 堆</span></span>
<span class="line"><span>  → 配合 V8 内对象的指针拿到任意读写</span></span>
<span class="line"><span>  → 通过 WebAssembly 拿 RWX 页 → RCE</span></span>
<span class="line"><span></span></span>
<span class="line"><span>攻击模式 2:渲染进程 DOM UAF</span></span>
<span class="line"><span>  → JS 触发 DOM 节点释放但还有引用</span></span>
<span class="line"><span>  → JS 立刻分配相同 size 的 ArrayBuffer 占位</span></span>
<span class="line"><span>  → 接下来 DOM 操作访问那个&quot;节点&quot;,其实是攻击者 buffer</span></span>
<span class="line"><span>  → 改 vtable → 跳 ROP → 渲染进程沙箱内 RCE</span></span>
<span class="line"><span></span></span>
<span class="line"><span>攻击模式 3:iOS Safari → 内核</span></span>
<span class="line"><span>  → WebKit 渲染进程 RCE(同上)</span></span>
<span class="line"><span>  → 内核驱动 UAF(IOMobileFrameBuffer / AppleAVD 等)</span></span>
<span class="line"><span>  → 沙箱逃逸 → 内核读写原语 → root</span></span></code></pre></div><p><strong>每一年 Pwn2Own / 国家级 APT 拿出的 0day,本质都是这两三个模式的变体</strong>。</p><hr><h2 id="十、修与防-工程师该做什么" tabindex="-1">十、修与防:工程师该做什么 <a class="header-anchor" href="#十、修与防-工程师该做什么" aria-label="Permalink to &quot;十、修与防:工程师该做什么&quot;">​</a></h2><h3 id="_10-1-代码层短期-长期" tabindex="-1">10.1 代码层短期 / 长期 <a class="header-anchor" href="#_10-1-代码层短期-长期" aria-label="Permalink to &quot;10.1 代码层短期 / 长期&quot;">​</a></h3><p><strong>短期(C/C++ 项目立刻能做)</strong>:</p><ul><li><code>free(p); p = NULL;</code> —— 释放后立刻置空,消灭 dangling pointer。代价 0,效果显著。</li><li><code>delete</code> 之后用 <code>unique_ptr / shared_ptr</code> —— 让所有权语义在编译期表达,<strong>不再手动 free</strong>。</li><li>任何 <code>memcpy / strcpy</code> 替换成带长度版本(<code>memcpy_s / strncpy_s</code>)。</li><li>开 <code>-D_FORTIFY_SOURCE=3</code> <code>-fstack-protector-strong</code> <code>-fsanitize=address</code>(debug 时)。</li><li>CI 接 ASan / UBSan / MSan —— <strong>本地跑一遍单测就能抓出 90% 的 UAF / overflow</strong>。</li></ul><p><strong>长期(项目级)</strong>:</p><ul><li>新模块用 Rust 写 —— Mozilla / Microsoft / Google / Apple 都在做。Rust 编译期 ownership 检查直接消灭 UAF / Double Free / Iterator invalidation,<strong>这一类漏洞在 Rust 里写不出来</strong>。</li><li>不能换 Rust 的存量 C++ 代码 —— 上 MiraclePtr / 智能指针 / Chromium 风格的所有权审查。</li><li>漏洞类别消除(VRP class elimination)—— Google 内部做法:每次新发现一类漏洞,<strong>不是修一个,是问&quot;怎么让这一类整体消失&quot;</strong>。Chrome 的 V8 Sandbox 就是这种思路。</li></ul><h3 id="_10-2-系统-编译层缓解" tabindex="-1">10.2 系统 / 编译层缓解 <a class="header-anchor" href="#_10-2-系统-编译层缓解" aria-label="Permalink to &quot;10.2 系统 / 编译层缓解&quot;">​</a></h3><p>19 篇会专门讲——这里只列堆相关的:</p><ul><li><strong>ASLR</strong>:堆基址随机化,提高 spray 难度。</li><li><strong>DEP / W^X</strong>:堆页不可执行,杀死直接 shellcode 喷射(但 ROP 没事)。</li><li><strong>Safe-Linking</strong>(glibc 2.32+):tcache fd 指针带 cookie。</li><li><strong>CFI</strong>(Clang / MSVC):虚函数调用做合法 vtable 集合检查。</li><li><strong>MTE</strong>(ARMv9):硬件级 16 字节粒度的 tag 检查。</li><li><strong>scudo / hardened malloc</strong>:替换默认分配器,加上 quarantine / guard page / 随机化。</li></ul><p><strong>没有一个能单独杀死堆漏洞</strong>——<strong>纵深防御才是真实路径</strong>:开发期(智能指针 + Rust)→ 编译期(CFI + Sanitizer)→ 运行期(MTE + scudo)→ 进程间(沙箱)→ 系统(内核 hardening)。<strong>任何一层独自被绕过都不会让攻击者拿到完整 RCE</strong>。</p><h3 id="_10-3-监控与应急" tabindex="-1">10.3 监控与应急 <a class="header-anchor" href="#_10-3-监控与应急" aria-label="Permalink to &quot;10.3 监控与应急&quot;">​</a></h3><ul><li>用户态崩溃日志(coredump / crashpad)接 Crashpad / Breakpad → 中心化收集 → <strong>同一个崩溃栈短时间高频出现 = 可能是被野外利用</strong>。</li><li>AddressSanitizer 在生产环境关掉,但<strong>有些公司在 Canary 环境长期开</strong>——以可观测的崩溃换&quot;线上漏洞被外部攻击者先发现&quot;的风险。</li><li>应急响应:堆漏洞 0day 一旦野外公开,<strong>第一反应是更新基础库 / 重启服务</strong>——很多 glibc / OpenSSL 漏洞通过升级就能修,但<strong>升级不重启,内存里跑的还是老代码</strong>。</li></ul><hr><h2 id="十一、踩坑提醒" tabindex="-1">十一、踩坑提醒 <a class="header-anchor" href="#十一、踩坑提醒" aria-label="Permalink to &quot;十一、踩坑提醒&quot;">​</a></h2><ol><li><strong><code>free(p)</code> 之后 <code>p</code> 还能用</strong>——C/C++ 不会自动置空,21 世纪最贵的 NULL。</li><li><strong><code>if (p) free(p)</code></strong>——这句没意义,<code>free(NULL)</code> 本来就合法;真正该写的是 <code>free(p); p = NULL;</code>。</li><li><strong>以为加了 Canary / ASLR / DEP 就安全</strong>——这些对栈漏洞够用,<strong>对堆只是延缓</strong>,需要 CFI + MiraclePtr + MTE 这套现代组合。</li><li><strong>以为 <code>unique_ptr</code> 万能</strong>——<code>unique_ptr.get()</code> 返回的裸指针、<code>.release()</code> 之后的所有权转移、<code>shared_ptr</code> 循环引用,<strong>每一个都是漏洞温床</strong>。</li><li><strong>以为 Rust 完全消除内存漏洞</strong>——<code>unsafe</code> 块 / FFI 边界 / 逻辑 use-after-free(借助 <code>Rc&lt;RefCell&gt;</code> 也能写出 UAF 等价物),<strong>Rust 是消除 90%,不是 100%</strong>。</li><li><strong>以为 Sanitizer = 测试通过 = 没漏洞</strong>——ASan 只在被实际触发的代码路径上检测,<strong>模糊测试覆盖不到的代码,Sanitizer 也帮不上</strong>。20 篇会讲 fuzzing。</li><li><strong>以为 Heartbleed 之后 OpenSSL 没问题了</strong>—— OpenSSL 后来又出了好几个堆相关 CVE,<strong>密码学库 + C 语言这个组合本质上就高风险</strong>。</li><li><strong>以为浏览器升级慢一点没事</strong>——Chrome 主版本 4 周一次、紧急 0day patch 24 小时内,<strong>慢 1 周可能就被野外打</strong>。企业 IT 把浏览器自动更新关掉是常见但极其错误的策略。</li><li><strong>以为容器隔离能挡住堆漏洞</strong> —— 浏览器渲染进程内的 UAF 不需要打到内核就能偷 Cookie / localStorage,<strong>进程级沙箱不是网络级隔离的替代</strong>。</li><li><strong>以为 Java / Python / Node 没有堆漏洞</strong>——这些语言的运行时本身是 C/C++ 写的,<strong>V8 / CPython / OpenJDK 自己被打就等于你的应用被打</strong>。2021 年 log4shell 不是堆漏洞,但有过 JNI 堆漏洞、cpython 解释器漏洞,<strong>只是离应用工程师远一点</strong>。</li></ol><hr><p>下一篇:<code>19-内存安全缓解机制.md</code>,把这一篇和 17 篇里反复提到的 ASLR / DEP / Canary / CFI / Shadow Stack / MTE 一次性讲透——为什么每一层缓解都&quot;只解决一类问题、不解决根本&quot;,为什么&quot;防御方付出 5% 性能换攻击者付出 10 倍精力&quot;是工程上正确的折中,以及 Intel CET 和 ARM PAC 这一代硬件辅助到底改变了什么。</p>`,144)])])}const k=a(t,[["render",p]]);export{g as __pageData,k as default};
