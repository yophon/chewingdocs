import{_ as i,H as s,f as e,i as t}from"./chunks/framework.BHvCMIhP.js";const k=JSON.parse('{"title":"底层大起底：Slice 与 Map 是怎么实现的","description":"","frontmatter":{},"headers":[],"relativePath":"../goLearning/10-底层大起底：Slice与Map是怎么实现的.md","filePath":"../goLearning/10-底层大起底：Slice与Map是怎么实现的.md","lastUpdated":1778496697000}'),n={name:"../goLearning/10-底层大起底：Slice与Map是怎么实现的.md"};function l(p,a,o,r,c,h){return s(),e("div",null,[...a[0]||(a[0]=[t(`<h1 id="底层大起底-slice-与-map-是怎么实现的" tabindex="-1">底层大起底：Slice 与 Map 是怎么实现的 <a class="header-anchor" href="#底层大起底-slice-与-map-是怎么实现的" aria-label="Permalink to &quot;底层大起底：Slice 与 Map 是怎么实现的&quot;">​</a></h1><blockquote><p><strong>导读</strong>：面试最爱问的核心考点。不了解底层，很容易写出内存泄漏和导致 Panic 的代码。</p></blockquote><h2 id="一、slice-切片-底层结构" tabindex="-1">一、Slice（切片）底层结构 <a class="header-anchor" href="#一、slice-切片-底层结构" aria-label="Permalink to &quot;一、Slice（切片）底层结构&quot;">​</a></h2><p>Slice 是一个极其轻量级的结构体（24 字节，在 64 位机器上）：</p><div class="language-go vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">go</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> slice</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    array </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">unsafe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Pointer</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // 指向底层真实数据的指针 (8 字节)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    len   </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            // 当前元素个数 (8 字节)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    cap   </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            // 容量，底层数组还能装多少 (8 字节)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><strong>扩容机制</strong>：</p><ul><li>当 <code>append</code> 超出 <code>cap</code> 时：如果当前容量小于 256 (Go 1.18 之后变动)，翻倍扩容；大于 256 后，平滑按一定比例递增扩容。</li><li><strong>陷阱</strong>：大数组切出一个小 Slice。只要小 Slice 还存活着，底层的庞大数组就无法被 GC 回收（内存泄漏）。</li><li><strong>解法</strong>：当只需要引用一小部分数据时，不要用切片语法 <code>arr[:10]</code>，而是新建一个 Slice 用 <code>copy()</code> 把数据拷过去。</li></ul><h2 id="二、map-哈希表-底层结构" tabindex="-1">二、Map（哈希表）底层结构 <a class="header-anchor" href="#二、map-哈希表-底层结构" aria-label="Permalink to &quot;二、Map（哈希表）底层结构&quot;">​</a></h2><p>Go 的 map 实现是一个 <code>hmap</code> 结构体，内部由多个 <code>bmap</code>（桶/Bucket）构成。</p><p><strong>核心机制</strong>：</p><ul><li>哈希冲突解决：采用<strong>拉链法</strong>。当一个 Bucket 塞满 8 个元素后，会挂上一个溢出桶（overflow bucket）。</li><li><strong>渐进式扩容</strong>：当装载因子（LoadFactor）&gt; 6.5 或溢出桶太多时，会触发翻倍扩容或等量扩容。Go 不会一次性搬迁数据（会卡死），而是在后续的插入、删除、查询操作中，一点点把老桶的数据搬迁到新桶（渐进式）。</li></ul><p><strong>使用禁忌</strong>：</p><ol><li>Map 不能边遍历边删除（某些情况下会导致死锁或崩溃）。</li><li>Map 不是并发安全的。如果你开两个 Goroutine，一个写 map，一个读/写 map，Go 运行时会直接报错 <code>fatal error: concurrent map read and map write</code>，导致程序强行退出，<code>recover</code> 都拦不住！必须使用 <code>sync.Mutex</code> 或者直接用标准库提供的并发安全 <code>sync.Map</code>。</li></ol>`,13)])])}const g=i(n,[["render",l]]);export{k as __pageData,g as default};
