import{c as a,Q as e,j as n,m as t}from"./chunks/framework.Bhbi9jCp.js";const g=JSON.parse('{"title":"原生 iOS 的一张地图","description":"","frontmatter":{},"headers":[],"relativePath":"iosNativeLearning/01-原生iOS的一张地图.md","filePath":"iosNativeLearning/01-原生iOS的一张地图.md","lastUpdated":1779789238000}'),o={name:"iosNativeLearning/01-原生iOS的一张地图.md"};function i(p,s,d,l,c,r){return e(),n("div",null,[...s[0]||(s[0]=[t(`<h1 id="原生-ios-的一张地图" tabindex="-1">原生 iOS 的一张地图 <a class="header-anchor" href="#原生-ios-的一张地图" aria-label="Permalink to &quot;原生 iOS 的一张地图&quot;">​</a></h1><p>学 iOS 的最大障碍不是&quot;API 太多&quot;,是<strong>心智模型没建起来</strong>。一个写过几年 Objective-C + Storyboard 的人,如果还把 iOS 当&quot;多个 <code>UIViewController</code> 通过 segue 互跳&quot;,那一打开 SwiftUI 项目就会立刻懵——<code>@main App</code>?<code>some View</code>?<code>@State</code>?导航怎么 push?这一篇不教任何 API,只讲一件事:<strong>现代 iOS 到底长什么样</strong>。</p><blockquote><p>一句话先记住:<strong>现代 iOS 是一棵被 Observation 追踪的 SwiftUI 视图树,挂在 <code>App</code> 协议入口上,被 Swift Concurrency 隔离并发,被 SwiftData 落盘,通过 Apple 平台框架接入系统能力。其余 framework——Widget、推送、StoreKit、Core ML——都是这棵树的扩展或副作用边界。</strong> 这句话听上去很抽象,但后面 26 篇都是它的推论。</p></blockquote><hr><h2 id="一、忘掉-storyboard-uikit-那一代-ios" tabindex="-1">一、忘掉 Storyboard + UIKit 那一代 iOS <a class="header-anchor" href="#一、忘掉-storyboard-uikit-那一代-ios" aria-label="Permalink to &quot;一、忘掉 Storyboard + UIKit 那一代 iOS&quot;">​</a></h2><p>很多人对 iOS 的初始印象是&quot;Objective-C 写业务、Storyboard 拖界面、<code>UIViewController</code> 组合生命周期、GCD 跑后台&quot;。这套心智在 2018 年之前是对的,在 2026 年是<strong>陈旧的</strong>。</p><table tabindex="0"><thead><tr><th>维度</th><th>旧 iOS(2010-2018)</th><th>现代 iOS(2022-)</th></tr></thead><tbody><tr><td>语言</td><td>Objective-C / Swift 5</td><td><strong>Swift 6</strong>(strict concurrency)</td></tr><tr><td>UI</td><td>Storyboard / XIB + Auto Layout</td><td><strong>SwiftUI</strong>(声明式)</td></tr><tr><td>屏幕</td><td>每屏一个 <code>UIViewController</code>,<code>segue</code> 跳转</td><td><strong><code>NavigationStack</code></strong> + 类型化路由</td></tr><tr><td>入口</td><td><code>AppDelegate</code> + <code>SceneDelegate</code></td><td><strong><code>@main</code> + <code>App</code> 协议</strong></td></tr><tr><td>异步</td><td><code>dispatch_async</code> / <code>OperationQueue</code></td><td><strong><code>async/await</code> + <code>actor</code></strong></td></tr><tr><td>状态</td><td><code>delegate</code> + KVO + Notification</td><td><strong><code>@State</code> + <code>@Observable</code></strong>(字段级追踪)</td></tr><tr><td>数据层</td><td>Core Data NSManaged 一堆样板</td><td><strong>SwiftData</strong>(<code>@Model</code> 几行搞定)</td></tr><tr><td>网络</td><td><code>NSURLConnection</code> → <code>URLSession</code> callback</td><td><strong><code>URLSession.data(for:) async</code></strong></td></tr><tr><td>包管理</td><td>CocoaPods / Carthage</td><td><strong>Swift Package Manager</strong>(Xcode 原生)</td></tr><tr><td>发布</td><td>iTunes Connect 手动 archive</td><td><strong>App Store Connect + Xcode Cloud / TestFlight</strong></td></tr></tbody></table><p><strong>最要命的差别不在 API 数量,而在心智方向</strong>:旧 iOS 是&quot;命令式 + 引用驱动&quot;,你拿到 <code>UILabel</code> 引用、<code>label.text = &quot;hello&quot;</code> 命令式修改。现代 iOS 是&quot;声明式 + 状态驱动&quot;,你声明的是&quot;当状态长这样时 UI 应该长这样&quot;,剩下的让 SwiftUI runtime 自己 diff 出最小重渲染范围。</p><blockquote><p>如果你脑子里 iOS 的心智还停留在&quot;<code>viewDidLoad</code> / <code>viewWillAppear</code> / <code>viewDidDisappear</code> 五段生命周期&quot;,那看 SwiftUI 时会一直问&quot;这哪里相当于 viewDidLoad&quot;,看 <code>@Observable</code> 时会一直问&quot;它什么时候 dealloc&quot;——问题不在 API,在你以为 iOS 还是 2015 年那一套。</p></blockquote><hr><h2 id="二、那棵树长什么样" tabindex="-1">二、那棵树长什么样 <a class="header-anchor" href="#二、那棵树长什么样" aria-label="Permalink to &quot;二、那棵树长什么样&quot;">​</a></h2><p>现代 iOS 应用,<strong>整个进程从一个 <code>@main App</code> 类型起步</strong>。这个 <code>App</code> 的 <code>body</code> 返回一组 <code>Scene</code>,<code>Scene</code> 里挂着 SwiftUI 视图树。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>进程</span></span>
<span class="line"><span>└── @main NotesIslandApp: App</span></span>
<span class="line"><span>    └── var body: some Scene</span></span>
<span class="line"><span>        └── WindowGroup { ... }       ← Scene,跨 form factor 抽象</span></span>
<span class="line"><span>            └── RootView()             ← SwiftUI 视图根节点</span></span>
<span class="line"><span>                ├── NavigationStack    ← 路由,管&quot;现在显示哪一屏&quot;</span></span>
<span class="line"><span>                │   ├── NoteListView()</span></span>
<span class="line"><span>                │   │   ├── List</span></span>
<span class="line"><span>                │   │   │   └── NoteRow(...) × N</span></span>
<span class="line"><span>                │   │   └── .toolbar { ... }</span></span>
<span class="line"><span>                │   └── NoteDetailView(...)</span></span>
<span class="line"><span>                └── .modelContainer(container)  ← SwiftData 注入</span></span></code></pre></div><p>每个节点(<code>RootView</code>、<code>NoteListView</code>、<code>NoteRow</code>)都是一个 <code>struct</code> 实现 <code>View</code> 协议。这些结构体<strong>只读状态、生成 View 描述</strong>,自己不持有任何字段引用、不订阅生命周期、不操作 <code>UIView</code>。</p><p><strong>这棵树的关键性质</strong>:</p><ol><li><strong>声明式</strong>——你写&quot;在某状态下 UI 长什么样&quot;,不写&quot;如何把 UI 从 A 状态改成 B 状态&quot;。</li><li><strong>View 是值类型</strong>——<code>struct</code>,每次重算 <code>body</code> 都是构造一个新值,几乎零成本。</li><li><strong>状态外挂</strong>——<code>@State</code> 实际指向 SwiftUI 框架维护的&quot;存储槽&quot;,视图重建不会丢状态。</li><li><strong>没有 <code>UIView</code> 引用</strong>——你拿不到也不需要拿 <code>UILabel</code> 这种对象,只描述输出。</li></ol><p>后面 05-10 篇全在讲这棵树:怎么搭、怎么重算、怎么布局、怎么动画。</p><hr><h2 id="三、谁持有状态-state-与-observable" tabindex="-1">三、谁持有状态:@State 与 @Observable <a class="header-anchor" href="#三、谁持有状态-state-与-observable" aria-label="Permalink to &quot;三、谁持有状态:@State 与 @Observable&quot;">​</a></h2><p>View 是 <code>struct</code>,<strong>结构体不可变</strong>,那 <code>@State private var count = 0; count += 1</code> 怎么改自己?</p><p>答案是 <code>@State</code> 不是普通字段,是 property wrapper:它的实际值存在 SwiftUI 框架管理的&quot;存储槽&quot;里,struct 本身只持有一个指向存储槽的引用。所以<strong>视图被反复重建,但状态跟着 view identity 走,不会丢</strong>。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SwiftUI 框架</span></span>
<span class="line"><span>   │</span></span>
<span class="line"><span>   ├── StorageNode(&quot;NoteListView@root/0&quot;)</span></span>
<span class="line"><span>   │   ├── @State var searchText: String      ← 视图私有状态</span></span>
<span class="line"><span>   │   └── @State var presentingNewNote: Bool</span></span>
<span class="line"><span>   │</span></span>
<span class="line"><span>   └── EnvironmentValues</span></span>
<span class="line"><span>       └── modelContext: ModelContext         ← 跨视图依赖注入</span></span></code></pre></div><p><code>@State</code> 只够装&quot;视图私有的可变状态&quot;(搜索框输入、是否展开)。一旦状态要被多个视图共享、要跨页传递、要装真正的业务逻辑,就升级到 <code>@Observable</code>:</p><div class="language-swift vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">swift</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">@Observable</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">final</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> NotesStore</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    var</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> notes: [Note] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> []</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    var</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> isLoading </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> false</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    func</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> reload</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>@Observable</code> 宏展开后,SwiftUI 会<strong>按字段做依赖追踪</strong>——视图读了 <code>notes</code>,只有 <code>notes</code> 变化时它才重算;读了 <code>isLoading</code> 的另一个视图不受牵连。这是相对 <code>ObservableObject + @Published</code>(2019 老方案)的根本升级。06 篇展开。</p><blockquote><p>这就是 <strong>UDF(单向数据流)</strong> 在 SwiftUI 的落地:状态从 <code>@Observable</code> 流到 View,事件从 View 调回 <code>@Observable</code> 的方法,<strong>没有 <code>binding(to:)</code> 这种反向耦合</strong>。Combine 在 2026 年只保留在桥接 Notification / Timer 的边缘位置,不再是状态主轴。</p></blockquote><hr><h2 id="四、副作用从哪里出去-swift-concurrency" tabindex="-1">四、副作用从哪里出去:Swift Concurrency <a class="header-anchor" href="#四、副作用从哪里出去-swift-concurrency" aria-label="Permalink to &quot;四、副作用从哪里出去:Swift Concurrency&quot;">​</a></h2><p>UI 树是纯的,<code>@Observable</code> 也只是个状态容器,真正&quot;做事&quot;的是<strong>async 函数</strong>——发网络、查数据库、调系统服务。所有这些操作通过 <code>async/await</code> 写成结构化的调用链,在 <code>Task</code> 里启动,绑定到某个 <code>actor</code> 上避免数据竞争。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>@MainActor View</span></span>
<span class="line"><span>   ↓ Task { await store.reload() }</span></span>
<span class="line"><span>   ↓</span></span>
<span class="line"><span>@Observable NotesStore (默认 @MainActor)</span></span>
<span class="line"><span>   ↓ await api.fetchNotes()</span></span>
<span class="line"><span>   ↓</span></span>
<span class="line"><span>actor APIClient (后台 actor)</span></span>
<span class="line"><span>   ↓ try await URLSession.shared.data(for: req)</span></span>
<span class="line"><span>   ↓</span></span>
<span class="line"><span>内核 / 网络</span></span></code></pre></div><p><strong>结构化并发</strong>的核心:<code>Task</code> 有 scope,scope 被取消时 <code>Task</code> 自动取消,没有&quot;孤儿协程&quot;在后台烧 CPU。<code>actor</code> 把&quot;共享可变状态 + 串行访问&quot;封装成语言原语,<strong>编译器在 Swift 6 严格并发模式下会拒绝任何跨 actor 的不安全访问</strong>——数据竞争从&quot;运行时偶发崩&quot;变成&quot;编译期就报错&quot;。04 / 05 篇讲透。</p><p>GCD(<code>DispatchQueue.global().async { ... }</code>)在 2026 年只在和老 SDK 桥接时出现,新代码不再写。</p><hr><h2 id="五、apple-sdk-是一堆-framework-不是一个-全家桶" tabindex="-1">五、Apple SDK 是一堆 framework,不是一个&quot;全家桶&quot; <a class="header-anchor" href="#五、apple-sdk-是一堆-framework-不是一个-全家桶" aria-label="Permalink to &quot;五、Apple SDK 是一堆 framework,不是一个&quot;全家桶&quot;&quot;">​</a></h2><p>很多人以为 &quot;iOS SDK&quot; 是个安装包,装上 Xcode 就有所有功能可用。<strong>对了一半</strong>——Xcode 确实把所有 framework 都装好,但你要 <code>import</code> 才能用,且不同 framework 服务于不同层次。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SwiftUI                  ← UI 主框架</span></span>
<span class="line"><span>SwiftData / WidgetKit    ← 数据 / Widget(平台框架)</span></span>
<span class="line"><span>ActivityKit              ← Live Activity / Dynamic Island</span></span>
<span class="line"><span>StoreKit                 ← 内购 / 订阅</span></span>
<span class="line"><span>UserNotifications        ← 推送</span></span>
<span class="line"><span>AppIntents               ← Siri / Shortcuts / Spotlight 统一入口</span></span>
<span class="line"><span>Foundation / Observation ← 标准库</span></span>
<span class="line"><span>Combine                  ← 桥接 Notification / Timer</span></span>
<span class="line"><span>URLSession (in Foundation) / Network (low-level)</span></span>
<span class="line"><span>AVFoundation / PhotoKit  ← 相机 / 媒体</span></span>
<span class="line"><span>Core ML / Vision         ← 端侧 AI</span></span>
<span class="line"><span>CryptoKit                ← 加密</span></span>
<span class="line"><span>UIKit / AppKit           ← 老 UI 框架,SwiftUI 不存在某能力时回落</span></span></code></pre></div><p><strong>这些 framework 是按需 import 的</strong>。一个最小 iOS App 只 <code>import SwiftUI</code> 就能跑;一个完整产品大概 import 8-15 个。每多一个 framework,启动时绑定开销、二进制体积都会增加——所以<strong>Apple 平台框架不等于&quot;必须每个都用&quot;</strong>。</p><blockquote><p>这套分层和 Android 的 Jetpack 思路相反:Android 把&quot;现代化 API&quot;都放在 <code>androidx.*</code> 命名空间里独立版本化;Apple 的现代化 API 直接长在系统 framework 上,跟着 iOS 版本走。所以<strong>你能用什么 API 完全取决于 <code>IPHONEOS_DEPLOYMENT_TARGET</code>,不像 Android 可以用 AndroidX 把老设备拉到新能力</strong>。</p></blockquote><hr><h2 id="六、ios-swift-xcode-apple-developer-四件事" tabindex="-1">六、iOS / Swift / Xcode / Apple Developer 四件事 <a class="header-anchor" href="#六、ios-swift-xcode-apple-developer-四件事" aria-label="Permalink to &quot;六、iOS / Swift / Xcode / Apple Developer 四件事&quot;">​</a></h2><p>新读者最容易混淆这四个名词:</p><ul><li><strong>iOS</strong>——Apple 在 iPhone 上跑的操作系统,按主版本号发布,iOS 18 = 2024 秋发布,iOS 19 = 2025 秋发布。SDK 跟着系统走。</li><li><strong>Swift</strong>——Apple 维护的语言,2024-09 发布 Swift 6,2026 已经到 6.x。Swift 与 iOS 版本<strong>部分耦合</strong>:新 Swift 需要新 Xcode,新 Xcode 默认绑定某个最低 iOS SDK。</li><li><strong>Xcode</strong>——Apple 的 IDE,版本号比 iOS 慢半拍(Xcode 16 = 配 iOS 18)。它<strong>不只是 IDE</strong>,还是 SDK 发布载体——SDK 装在 <code>Xcode.app/Contents/Developer/Platforms/iPhoneOS.platform/</code>。</li><li><strong>Apple Developer Program</strong>——$99/年的开发者账号,签证书、上传 build、上架、推送都得有它。<strong>写代码不需要,真机调试 + 上架必须有</strong>。</li></ul><p>四件事的关系:<strong>Xcode 16 内置 Swift 6 编译器 + iOS 18 SDK,产出一个 <code>.ipa</code>,通过 Apple Developer 账号上传到 App Store Connect</strong>。</p><hr><h2 id="七、deployment-target-不是版本号-是契约" tabindex="-1">七、deployment target:不是版本号,是契约 <a class="header-anchor" href="#七、deployment-target-不是版本号-是契约" aria-label="Permalink to &quot;七、deployment target:不是版本号,是契约&quot;">​</a></h2><p><code>IPHONEOS_DEPLOYMENT_TARGET = 18.0</code> 这个 Build Setting 是 iOS 上最被低估的数字之一:</p><ul><li>它的意思是&quot;这个 App <strong>不允许装在 iOS 18.0 以下的设备上</strong>&quot;。App Store 会拦截低版本设备下载。</li><li>它<strong>同时声明了你能默认 import 的 API 集合</strong>:<code>@Observable</code> 是 iOS 17+,<code>@Entry</code> 宏是 iOS 18+,Foundation Model 是 iOS 19+。把 deployment target 设成 18,等于声明&quot;我代码里所有 ≤ iOS 18 的 API 都可以裸用,无需 <code>if #available</code>&quot;。</li></ul><p><code>@available(iOS 19, *)</code> 与 <code>if #available(iOS 19, *)</code> 配套使用,是&quot;我用了一个比 deployment target 更新的 API,我自觉给降级方案&quot;的标准姿势:</p><div class="language-swift vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">swift</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> #available</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">iOS</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 19</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 用 iOS 19+ 的 Foundation Model 端侧推理</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">else</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 降级:云端 API 调用</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><strong>没有 <code>if #available</code> 就裸用比 deployment target 更高的 API,运行到老系统上会直接 crash</strong>(framework 加载失败或符号缺失)。NotesIsland 钉死 <strong>deployment target = iOS 18</strong>,iOS 19+ API 永远走 <code>if #available</code>。</p><blockquote><p>选 deployment target 是一笔交易:往上提一档,失去一批用户;往下降一档,多一堆 <code>if #available</code>。2026/05 时点的甜点是 iOS 18(iOS 17 已经丢失装机量、iOS 19 又把 iPhone 11/12 切掉)。每年这个甜点位会移,选基线时查 <a href="https://developer.apple.com/support/app-store/" target="_blank" rel="noreferrer">Apple App Store 装机量</a>。</p></blockquote><hr><h2 id="八、本系列的整体地图" tabindex="-1">八、本系列的整体地图 <a class="header-anchor" href="#八、本系列的整体地图" aria-label="Permalink to &quot;八、本系列的整体地图&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>01     心智总览(这篇)</span></span>
<span class="line"><span>02-04  地基:Xcode 项目 → Swift 6 类型 → Concurrency 与 Sendable</span></span>
<span class="line"><span>       这一段是&quot;会写 iOS&quot; 的前提,顺序不能跳</span></span>
<span class="line"><span></span></span>
<span class="line"><span>05-10  SwiftUI 一族:View / 状态 / 布局 / Modifier / 动画 / 手势</span></span>
<span class="line"><span>       这一段是&quot;会写界面&quot; 的核心,占整本最大篇幅</span></span>
<span class="line"><span></span></span>
<span class="line"><span>11-14  架构与闭环:NavigationStack / SwiftData / URLSession / Keychain</span></span>
<span class="line"><span>       这一段读完,你能从零搭一个能联网能存数据能加密的 App</span></span>
<span class="line"><span></span></span>
<span class="line"><span>15-19  系统能力:互操作 / 推送 / 相机 / 权限 / 后台</span></span>
<span class="line"><span>       按需看,做什么功能看什么</span></span>
<span class="line"><span></span></span>
<span class="line"><span>20-23  平台特色与体验:Widget / 内购 / 端侧 AI / 无障碍</span></span>
<span class="line"><span>       iOS 区别于跨端的核心阵地</span></span>
<span class="line"><span></span></span>
<span class="line"><span>24-27  工程化与发布:性能 / Instruments / 模块化测试 / 签名上架</span></span>
<span class="line"><span>       上架前必看</span></span></code></pre></div><p><strong>优先级</strong>:01-14 每篇都建议看;15-19 按需;20-27 真上架前再回头读。</p><hr><h2 id="九、踩坑提醒-总览版-后面每篇细讲" tabindex="-1">九、踩坑提醒(总览版,后面每篇细讲) <a class="header-anchor" href="#九、踩坑提醒-总览版-后面每篇细讲" aria-label="Permalink to &quot;九、踩坑提醒(总览版,后面每篇细讲)&quot;">​</a></h2><ol><li><strong>把 SwiftUI 当 React 用</strong>——SwiftUI 的 diff 不是 VirtualDOM,View 是 struct,<code>@State</code> 不是 hook,误等同会算错重渲染成本,24 篇展开。</li><li><strong>多 <code>UIViewController</code> 心智迁移过来</strong>——<code>UIViewController</code> 在现代 SwiftUI 项目几乎不出现(除非桥接老 SDK),99% 屏幕用 <code>View</code> + <code>NavigationStack</code>,11 篇展开。</li><li><strong><code>@StateObject</code> / <code>ObservableObject</code> / <code>@Published</code> 还在写</strong>——2019 年的老 MVVM 方案已经被 <code>@Observable</code> 字段级追踪取代,06 篇会讲为什么这是性能升级而不是语法糖。</li><li><strong><code>DispatchQueue.main.async</code> 切回主线程</strong>——<code>async/await</code> + <code>@MainActor</code> 已经把这件事做掉,新代码不再写 GCD,04 篇展开。</li><li><strong>怕 <code>@unchecked Sendable</code>,也怕严格并发警告</strong>——Swift 6 严格并发不是来折腾你,是替你抓数据竞争。<code>@unchecked Sendable</code> 是把编译器的安全网关掉,等于&quot;我保证线程安全,出问题我背锅&quot;,新人绝对不要乱标,05 篇会讲怎么正面解决。</li><li><strong>deployment target 设成最新 iOS</strong>——会失去一批装机量;设成 iOS 15 又要写一堆 <code>if #available</code>。iOS 18 是 2026 的甜点,不要照搬旧教程。</li><li><strong>想接推送就退回 <code>AppDelegate</code> 模板</strong>——错的。<code>@main App</code> 入口加 <code>@UIApplicationDelegateAdaptor</code> 就能保留 push / URL scheme 等回调,16 篇讲。</li><li><strong>不读 Privacy Manifest 要求</strong>——2024 起 Apple 强制,审核会卡。第三方 SDK 也必须有,18 篇讲。</li><li><strong>以为 Combine 还是主流</strong>——SwiftUI 状态层已经被 <code>@Observable</code> 完全替代,Combine 在 2026 只剩&quot;桥接 <code>NotificationCenter</code> / <code>Timer</code> / Foundation 老 API&quot;这种边缘活,06 / 13 篇展开。</li><li><strong>沉迷三方库</strong>——AsyncImage 替代 Kingfisher,<code>Codable</code> 替代 SwiftyJSON,<code>URLSession</code> async 替代 Alamofire。新项目零三方依赖能撑很久,26 篇真要拆模块时再上 SPM。</li></ol><hr><p>下一篇 <code>02-Xcode项目骨架与xcconfig.md</code>,讲 Xcode 16 工程怎么从模板开始去掉模板坏味、<code>xcconfig</code> 怎么分环境配置、<code>Info.plist</code> generated 模式、Asset Catalog 与 SF Symbols、Build Settings 哪些必改、Workspace 与 Project 的边界。</p>`,60)])])}const u=a(o,[["render",i]]);export{g as __pageData,u as default};
