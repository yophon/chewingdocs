import{_ as a,H as n,f as t,i as p}from"./chunks/framework.BHvCMIhP.js";const d=JSON.parse('{"title":"栈溢出与 ROP:经典 stack smashing 与 Return-Oriented Programming","description":"","frontmatter":{},"headers":[],"relativePath":"securityLearning/17-栈溢出与ROP.md","filePath":"securityLearning/17-栈溢出与ROP.md","lastUpdated":1778496697000}'),i={name:"securityLearning/17-栈溢出与ROP.md"};function e(l,s,o,r,g,h){return n(),t("div",null,[...s[0]||(s[0]=[p(`<h1 id="栈溢出与-rop-经典-stack-smashing-与-return-oriented-programming" tabindex="-1">栈溢出与 ROP:经典 stack smashing 与 Return-Oriented Programming <a class="header-anchor" href="#栈溢出与-rop-经典-stack-smashing-与-return-oriented-programming" aria-label="Permalink to &quot;栈溢出与 ROP:经典 stack smashing 与 Return-Oriented Programming&quot;">​</a></h1><p>讲栈溢出最大的误区是「现在还有人写 C 吗?这玩意儿是上个世纪的事了」——<strong>这种轻视是栈溢出至今活着的根本原因</strong>。Linux 内核、glibc、OpenSSL、SQLite、ffmpeg、所有浏览器内核、所有数据库引擎,<strong>全部是 C/C++ 写的</strong>——你今天在用的每一个云服务,底下都有几千万行 C 代码。栈溢出从 1988 年 Morris 蠕虫开始,到 1996 年 Aleph One 把&quot;配方&quot;写明白,<strong>整整三十年里它依然是 CVE 排行榜常客</strong>——只是攻击姿势从「注入 shellcode 就能跑」进化到了「用程序里已有的小片段拼出新逻辑」。这一篇讲清楚:<strong>栈是怎么被打穿的、为什么 DEP/NX 没能终结这个故事、ROP 怎么用一堆 <code>ret</code> 指令拼出图灵完备的攻击逻辑、以及为什么 Rust / Go / Java 几乎不会出这类漏洞</strong>。</p><blockquote><p>一句话先记住:<strong>栈溢出 = 把数据写超出缓冲区边界,把&quot;控制流的元数据(返回地址 / 函数指针)&quot;覆盖成攻击者想要的值</strong>——本质和 SQL 注入同源,<strong>代码和数据没分开</strong>。<strong>ROP = DEP 把数据页设成不可执行后,攻击者不再注入新代码,而是把程序自己已有的代码片段(以 <code>ret</code> 结尾的几条指令,叫 gadget)串成一条新的&quot;执行路径&quot;</strong> —— 就像绑票电影里那个用报纸剪字拼威胁信的桥段,<strong>字都是别人的,意思是你的</strong>。</p></blockquote><blockquote><p><strong>本篇严格只讲原理 + payload 大致结构,不给完整可运行 exploit</strong>。所有动手练习请在<strong>自己搭的靶机或 CTF 环境</strong>里做,别打别人系统——这不是道德问题,是法律问题(《刑法》285、286 条,美国 CFAA 等)。</p></blockquote><hr><h2 id="一、栈溢出为什么三十年杀不死" tabindex="-1">一、栈溢出为什么三十年杀不死 <a class="header-anchor" href="#一、栈溢出为什么三十年杀不死" aria-label="Permalink to &quot;一、栈溢出为什么三十年杀不死&quot;">​</a></h2><h3 id="_1-1-根本原因-c-的内存模型把-数据-和-控制-放在一起" tabindex="-1">1.1 根本原因:C 的内存模型把&quot;数据&quot;和&quot;控制&quot;放在一起 <a class="header-anchor" href="#_1-1-根本原因-c-的内存模型把-数据-和-控制-放在一起" aria-label="Permalink to &quot;1.1 根本原因:C 的内存模型把&quot;数据&quot;和&quot;控制&quot;放在一起&quot;">​</a></h3><p>回看 16 篇讲的栈帧布局——<strong>调用一个函数时,返回地址和局部变量挨在一起,而且写入方向相反</strong>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>高地址  ┌──────────────────┐</span></span>
<span class="line"><span>        │  函数参数         │</span></span>
<span class="line"><span>        ├──────────────────┤</span></span>
<span class="line"><span>        │  返回地址(rip)   │ ← 函数执行完跳回这里</span></span>
<span class="line"><span>        ├──────────────────┤</span></span>
<span class="line"><span>        │  保存的 rbp       │</span></span>
<span class="line"><span>        ├──────────────────┤</span></span>
<span class="line"><span>        │  局部变量 buf[64] │ ← 写入从低地址往高地址</span></span>
<span class="line"><span>        │  ...              │</span></span>
<span class="line"><span>低地址  └──────────────────┘</span></span></code></pre></div><p><strong>关键事实</strong>:<code>buf</code> 写入方向是「从低地址往高地址」,<strong>朝着返回地址的方向</strong>。<code>strcpy</code> / <code>gets</code> / <code>sprintf</code> / <code>memcpy</code> 这些函数<strong>不做边界检查</strong>——你说要拷 200 字节,它就拷 200 字节,根本不管目标缓冲区只有 64 字节。<strong>多出来的 136 字节就把返回地址、保存的 rbp、甚至上层栈帧全覆盖了</strong>。</p><p><strong>这是 C 设计哲学的代价</strong>——C 把性能放在第一位,<strong>边界检查交给程序员</strong>。1972 年 Unix 还跑在几兆内存的 PDP-11 上,<strong>每次访问数组都做边界检查代价太高</strong>,Dennis Ritchie 选择「相信程序员」。<strong>这个选择影响了五十年</strong>——后果就是栈溢出 / 堆溢出 / use-after-free 这三大类内存安全漏洞,<strong>至今占微软 / 谷歌产品 CVE 总数的 70%</strong> (2019 微软安全响应中心数据)。</p><h3 id="_1-2-历史时刻-1988-morris-蠕虫-1996-aleph-one" tabindex="-1">1.2 历史时刻:1988 Morris 蠕虫 / 1996 Aleph One <a class="header-anchor" href="#_1-2-历史时刻-1988-morris-蠕虫-1996-aleph-one" aria-label="Permalink to &quot;1.2 历史时刻:1988 Morris 蠕虫 / 1996 Aleph One&quot;">​</a></h3><p><strong>Morris Worm(1988-11)</strong> 是栈溢出的&quot;出道作品&quot;——Robert Morris 利用 BSD <code>fingerd</code> 服务的栈溢出,<strong>让 10% 的当时联网计算机瘫痪</strong>(几千台,占当时整个互联网)。这是计算机犯罪入刑的第一案。Morris 后来去 MIT 当了教授,但栈溢出从此进了攻击者的工具箱。</p><p><strong>Aleph One「Smashing the Stack for Fun and Profit」(Phrack 49, 1996-11)</strong> 是把「栈溢出怎么写 exploit」<strong>完整流程写出来的开山之作</strong>。在这之前,栈溢出是少数大佬的秘技;之后,<strong>任何会写 C 的人读完都能复现</strong>。这篇文章的核心心智是:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. 找一个边界检查缺失的函数(strcpy / gets / sprintf 等)</span></span>
<span class="line"><span>2. 算出 buf 到返回地址的偏移(填多少字节才覆盖到)</span></span>
<span class="line"><span>3. 把&quot;自己写的 shellcode 机器码&quot;放进 buf</span></span>
<span class="line"><span>4. 把返回地址覆盖成 buf 的地址</span></span>
<span class="line"><span>5. 函数返回时 CPU 跳进 buf 开始执行 shellcode</span></span>
<span class="line"><span>6. shellcode 调用 execve(&quot;/bin/sh&quot;) → 拿到 shell</span></span></code></pre></div><p><strong>这个配方在 90 年代到 2000 年代初屡试不爽</strong>——Code Red / Slammer / Blaster 几个改变互联网历史的蠕虫,<strong>全是栈溢出</strong>。直到 OS 厂商加上了 DEP/NX,故事才进入下一章。</p><h3 id="_1-3-第三层原因-不安全函数至今没绝迹" tabindex="-1">1.3 第三层原因:不安全函数至今没绝迹 <a class="header-anchor" href="#_1-3-第三层原因-不安全函数至今没绝迹" aria-label="Permalink to &quot;1.3 第三层原因:不安全函数至今没绝迹&quot;">​</a></h3><div class="language-c vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 教科书级的&quot;全家桶毒药&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">gets</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(buf);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">             // ← C11 已经把它从标准里删了,但二进制还在</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">strcpy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(dst, src);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // ← src 多长它拷多长</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">strcat</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(dst, src);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // ← 同上</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sprintf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(buf, fmt, x);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // ← 不知道 buf 多大</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">scanf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%s</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, buf);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // ← %s 不限长度</span></span></code></pre></div><p><strong>安全替代品早就有</strong>:<code>strncpy</code> / <code>snprintf</code> / <code>fgets</code> / <code>strlcpy</code>,但是:</p><ol><li><strong>老代码迁移成本高</strong>——Linux 内核 / glibc / OpenSSL 几千万行,改不完</li><li><strong>新写的代码依然有人用</strong>——Stack Overflow 上的&quot;快速答案&quot;很多是不安全版本</li><li><strong>&quot;安全版本&quot;自己也有坑</strong>——<code>strncpy</code> 如果源串塞满了 N 字节,<strong>不会写 <code>\\0</code></strong>,后续读出来又溢了</li></ol><blockquote><p><strong>教训</strong>:<strong>单靠&quot;教育程序员&quot;无法解决内存安全问题</strong>——这是为什么业界最终把希望寄托在「内存安全语言」上(见第七节)。</p></blockquote><hr><h2 id="二、经典-stack-smashing-把返回地址改写成想要的值" tabindex="-1">二、经典 stack smashing:把返回地址改写成想要的值 <a class="header-anchor" href="#二、经典-stack-smashing-把返回地址改写成想要的值" aria-label="Permalink to &quot;二、经典 stack smashing:把返回地址改写成想要的值&quot;">​</a></h2><h3 id="_2-1-最小-vulnerable-c-代码" tabindex="-1">2.1 最小 vulnerable C 代码 <a class="header-anchor" href="#_2-1-最小-vulnerable-c-代码" aria-label="Permalink to &quot;2.1 最小 vulnerable C 代码&quot;">​</a></h3><div class="language-c vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// vuln.c —— 故意写错,只用于教学</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;string.h&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;stdio.h&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> win</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">puts</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;you won!&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">); }</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // 攻击者想跳到这</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> greet</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">char</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    char</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> buf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">];</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    strcpy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(buf, name);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   // ← 没检查长度,危险!</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    printf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;hello </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%s\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, buf);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> main</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> argc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">char</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> **</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">argv</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    greet</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">argv</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>编译命令(只在自己机器上做练习):</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">gcc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -fno-stack-protector</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -no-pie</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -z</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> execstack</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -O0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> vuln.c</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -o</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> vuln</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#    ↑关闭Canary       ↑关ASLR ↑栈可执行(模拟90年代环境)</span></span></code></pre></div><p><strong>这条命令本身就在告诉你</strong>:现代编译器<strong>默认就开了几道保护</strong>,你要&quot;复现 1996 年的世界&quot;必须显式关掉——这就是后面要讲的 Canary / ASLR / NX。</p><h3 id="_2-2-栈布局-进入-greet-之后" tabindex="-1">2.2 栈布局:进入 greet 之后 <a class="header-anchor" href="#_2-2-栈布局-进入-greet-之后" aria-label="Permalink to &quot;2.2 栈布局:进入 greet 之后&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>高地址                                       高地址</span></span>
<span class="line"><span>┌────────────────┐                          ┌────────────────┐</span></span>
<span class="line"><span>│  argv[1] 指针  │                          │  argv[1] 指针  │</span></span>
<span class="line"><span>├────────────────┤                          ├────────────────┤</span></span>
<span class="line"><span>│  返回地址 0x...│  ← main 调 greet 时压入  │  AAAAAAAA      │ ← 被覆盖!</span></span>
<span class="line"><span>├────────────────┤                          ├────────────────┤</span></span>
<span class="line"><span>│  保存的 rbp    │                          │  AAAAAAAA      │</span></span>
<span class="line"><span>├────────────────┤                          ├────────────────┤</span></span>
<span class="line"><span>│                │                          │                │</span></span>
<span class="line"><span>│  buf[64]       │  ← strcpy 写入起点       │  AAAAAAAA....  │ ← payload</span></span>
<span class="line"><span>│                │                          │                │</span></span>
<span class="line"><span>└────────────────┘ 低地址                   └────────────────┘</span></span>
<span class="line"><span>   正常状态                                    被打穿后</span></span></code></pre></div><p><strong>payload 的关键结构</strong>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[ 64 字节填充 ][ 8 字节占 rbp 位置 ][ 8 字节 = win 函数地址 ]</span></span>
<span class="line"><span>       ↑                ↑                      ↑</span></span>
<span class="line"><span>   随便什么字符     随便什么字符        关键 —— greet 返回时跳到这</span></span></code></pre></div><p><strong>用 <code>objdump -d vuln | grep win</code> 拿到 <code>win</code> 函数地址</strong>,假设是 <code>0x401156</code>,<strong>填到第 73-80 字节(小端序)</strong>。运行:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./vuln</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python3</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;print(&quot;A&quot;*72 + &quot;\\x56\\x11\\x40\\x00\\x00\\x00\\x00\\x00&quot;)&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># hello AAAAA...</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># you won!  ← 控制流被劫持,跳到了 win</span></span></code></pre></div><p><strong>这就是栈溢出的本质</strong>——<strong>任何能控制返回地址的人,都能控制 CPU 接下来执行哪段代码</strong>。</p><h3 id="_2-3-90-年代版-注入-shellcode" tabindex="-1">2.3 90 年代版:注入 shellcode <a class="header-anchor" href="#_2-3-90-年代版-注入-shellcode" aria-label="Permalink to &quot;2.3 90 年代版:注入 shellcode&quot;">​</a></h3><p>「跳到 win」只是教学版,<strong>真实攻击是把一段机器码(shellcode)塞进 buf,然后把返回地址覆盖成 buf 的地址</strong>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>栈布局(经典 shellcode 版):</span></span>
<span class="line"><span>┌──────────────────────┐</span></span>
<span class="line"><span>│  返回地址 = buf 地址 │ ← greet 返回时跳到 buf</span></span>
<span class="line"><span>├──────────────────────┤</span></span>
<span class="line"><span>│  保存的 rbp(被盖)  │</span></span>
<span class="line"><span>├──────────────────────┤</span></span>
<span class="line"><span>│  buf[64]:            │</span></span>
<span class="line"><span>│  [ NOP NOP NOP ...]  │ ← NOP sled,落点稍微偏一点也没事</span></span>
<span class="line"><span>│  [ shellcode       ] │ ← execve(&quot;/bin/sh&quot;, 0, 0) 的机器码</span></span>
<span class="line"><span>│  ...                 │</span></span>
<span class="line"><span>└──────────────────────┘</span></span></code></pre></div><p><strong>NOP sled</strong> 是个经典技巧——一长串 <code>0x90</code>(NOP 指令),<strong>只要返回地址落在 sled 任意位置,CPU 一路 NOP 滑到 shellcode</strong>。这是为了对抗&quot;返回地址不好精确预测&quot;。</p><p><strong>shellcode 长什么样</strong>(只展示概念,不给完整):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>push  0x68732f6e ; &quot;n/sh&quot;</span></span>
<span class="line"><span>push  0x69622f2f ; &quot;//bi&quot;   ← 栈上拼出 &quot;//bin/sh&quot;</span></span>
<span class="line"><span>mov   rdi, rsp   ; argv[0] = &quot;//bin/sh&quot;</span></span>
<span class="line"><span>xor   rsi, rsi   ; argv = NULL</span></span>
<span class="line"><span>xor   rdx, rdx   ; envp = NULL</span></span>
<span class="line"><span>mov   rax, 59    ; sys_execve</span></span>
<span class="line"><span>syscall</span></span></code></pre></div><p><strong>这段二进制只有 23 字节,塞进 64 字节 buf 绰绰有余</strong>。这就是为什么 90 年代「一个 gets,一台机器」<strong>完全不夸张</strong>——攻击者拿到一台 root shell,接下来横向移动整个网络都是后话。</p><hr><h2 id="三、shellcode-时代为什么结束了-dep-nx" tabindex="-1">三、shellcode 时代为什么结束了:DEP / NX <a class="header-anchor" href="#三、shellcode-时代为什么结束了-dep-nx" aria-label="Permalink to &quot;三、shellcode 时代为什么结束了:DEP / NX&quot;">​</a></h2><h3 id="_3-1-dep-nx-是什么" tabindex="-1">3.1 DEP / NX 是什么 <a class="header-anchor" href="#_3-1-dep-nx-是什么" aria-label="Permalink to &quot;3.1 DEP / NX 是什么&quot;">​</a></h3><p><strong>DEP(Data Execution Prevention)</strong> / <strong>NX(No-eXecute)</strong> —— Intel 和 AMD 在 2003-2004 年加进 CPU 的硬件特性,<strong>操作系统利用页表给每个内存页打三个权限位:R(可读)/ W(可写)/ X(可执行)</strong>。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>原本的 x86:   只有 R / W 权限位,X 隐含 = R</span></span>
<span class="line"><span>现在的 x86_64: R W X 三个独立位 —— 一个页要么可写,要么可执行,不能同时</span></span></code></pre></div><p><strong>页表权限矩阵</strong>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>┌─────────────┬────┬────┬────┬────────────────┐</span></span>
<span class="line"><span>│  内存区域   │ R  │ W  │ X  │  说明          │</span></span>
<span class="line"><span>├─────────────┼────┼────┼────┼────────────────┤</span></span>
<span class="line"><span>│  .text 代码 │ Y  │ N  │ Y  │ 只读可执行     │</span></span>
<span class="line"><span>│  .rodata    │ Y  │ N  │ N  │ 字符串常量等   │</span></span>
<span class="line"><span>│  .data .bss │ Y  │ Y  │ N  │ 全局变量       │</span></span>
<span class="line"><span>│  栈          │ Y  │ Y  │ N  │ ← 关键!不可执行 │</span></span>
<span class="line"><span>│  堆          │ Y  │ Y  │ N  │ ← 关键!不可执行 │</span></span>
<span class="line"><span>└─────────────┴────┴────┴────┴────────────────┘</span></span></code></pre></div><p><strong>含义</strong>:<strong>栈上的数据再也不能被当作代码执行</strong>——shellcode 塞进 buf,CPU 跳过去时直接 segfault(权限错误)。<strong>1996 那一套配方瞬间失效</strong>。</p><h3 id="_3-2-看起来-完美-的防御-为什么没赢" tabindex="-1">3.2 看起来&quot;完美&quot;的防御,为什么没赢 <a class="header-anchor" href="#_3-2-看起来-完美-的防御-为什么没赢" aria-label="Permalink to &quot;3.2 看起来&quot;完美&quot;的防御,为什么没赢&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>攻击者视角:既然栈上的数据不能执行,</span></span>
<span class="line"><span>            那就不&quot;注入新代码&quot;,</span></span>
<span class="line"><span>            直接用&quot;已经在内存里、本来就可执行&quot;的代码!</span></span></code></pre></div><p><strong>关键洞察</strong>:程序的 <code>.text</code> 段和 libc 的代码段<strong>本来就是 X 权限</strong>——这些代码攻击者不能修改,但<strong>完全可以&quot;跳过去执行&quot;</strong>。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>传统 shellcode:           ROP:</span></span>
<span class="line"><span>[ buf 里塞机器码 ]        [ buf 里塞一串地址 ]</span></span>
<span class="line"><span>[ 跳到 buf 执行 ]          [ ret 依次跳到 libc 里的代码片段 ]</span></span>
<span class="line"><span>       ✗ NX 拦死          ✓ 这些地址全是 X 权限的现成代码</span></span></code></pre></div><p><strong>这就是 ROP 的诞生</strong>——<strong>Return-Oriented Programming</strong>,2007 年 Hovav Shacham 在 CCS 论文里系统化提出(更早有 ret2libc / return-into-libc 的雏形)。</p><hr><h2 id="四、rop-用别人的代码片段拼出新逻辑" tabindex="-1">四、ROP:用别人的代码片段拼出新逻辑 <a class="header-anchor" href="#四、rop-用别人的代码片段拼出新逻辑" aria-label="Permalink to &quot;四、ROP:用别人的代码片段拼出新逻辑&quot;">​</a></h2><h3 id="_4-1-什么是-gadget" tabindex="-1">4.1 什么是 gadget <a class="header-anchor" href="#_4-1-什么是-gadget" aria-label="Permalink to &quot;4.1 什么是 gadget&quot;">​</a></h3><p><strong>Gadget</strong> = <strong>几条指令 + 一个 <code>ret</code></strong> —— 这是 ROP 的最小积木。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>随便从 libc 里挖到的几个 gadget 例子:</span></span>
<span class="line"><span>0x4011a3:  pop rdi ; ret              ← 把栈顶值弹给 rdi 然后返回</span></span>
<span class="line"><span>0x4011a5:  pop rsi ; pop rdx ; ret    ← 弹两个值</span></span>
<span class="line"><span>0x4011a8:  xor rax, rax ; ret         ← 清零 rax</span></span>
<span class="line"><span>0x4011ab:  mov rax, 59 ; syscall      ← 触发 execve syscall</span></span></code></pre></div><p><strong>关键洞察</strong>:<code>ret</code> 指令的语义是「从栈顶弹一个地址,跳过去」。<strong>如果攻击者控制了栈,就控制了下一个 <code>ret</code> 跳哪去</strong>。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>攻击者把栈布置成这样:</span></span>
<span class="line"><span>┌──────────────────┐</span></span>
<span class="line"><span>│ gadget1 地址     │ ← 当前 ret 跳到这</span></span>
<span class="line"><span>├──────────────────┤</span></span>
<span class="line"><span>│ 给 gadget1 的参数│ ← gadget1 里的 pop 把这弹出去</span></span>
<span class="line"><span>├──────────────────┤</span></span>
<span class="line"><span>│ gadget2 地址     │ ← gadget1 的 ret 跳到这</span></span>
<span class="line"><span>├──────────────────┤</span></span>
<span class="line"><span>│ 给 gadget2 的参数│</span></span>
<span class="line"><span>├──────────────────┤</span></span>
<span class="line"><span>│ gadget3 地址     │</span></span>
<span class="line"><span>└──────────────────┘</span></span></code></pre></div><p><strong>每个 <code>ret</code> 都是一次&quot;跳转 + 自动推进栈指针&quot;</strong> —— 攻击者只要把栈控制权握在手里,就能把任意多个 gadget 串起来。<strong>这本质上是把 CPU 的执行流转化成了&quot;对栈这个数据结构的遍历&quot;</strong>。</p><h3 id="_4-2-一个最小-rop-chain-目标-execve-bin-sh-null-null" tabindex="-1">4.2 一个最小 ROP chain:目标 execve(&quot;/bin/sh&quot;, NULL, NULL) <a class="header-anchor" href="#_4-2-一个最小-rop-chain-目标-execve-bin-sh-null-null" aria-label="Permalink to &quot;4.2 一个最小 ROP chain:目标 execve(&quot;/bin/sh&quot;, NULL, NULL)&quot;">​</a></h3><p><strong>目标</strong>:在 x86_64 Linux 上调用 <code>execve</code> 系统调用拿 shell。需要做的事:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>rax = 59      ; syscall number for execve</span></span>
<span class="line"><span>rdi = &quot;/bin/sh&quot; 的地址</span></span>
<span class="line"><span>rsi = 0       ; argv</span></span>
<span class="line"><span>rdx = 0       ; envp</span></span>
<span class="line"><span>syscall</span></span></code></pre></div><p><strong>ROP chain 大致结构</strong>(只画结构,<strong>不给可运行 payload</strong>):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>栈布局(高地址在下,低地址在上,ret 一个个往下走):</span></span>
<span class="line"><span></span></span>
<span class="line"><span>┌──────────────────────────────────────────┐</span></span>
<span class="line"><span>│  填充字节 (64 + 8 字节,盖到返回地址前)  │</span></span>
<span class="line"><span>├──────────────────────────────────────────┤</span></span>
<span class="line"><span>│  &amp;(pop rdi ; ret)        ←  gadget #1   │  函数返回时第一个 ret 跳到这</span></span>
<span class="line"><span>├──────────────────────────────────────────┤</span></span>
<span class="line"><span>│  &amp;&quot;/bin/sh&quot;               ←  gadget #1 弹给 rdi</span></span>
<span class="line"><span>├──────────────────────────────────────────┤</span></span>
<span class="line"><span>│  &amp;(pop rsi ; pop rdx ; ret) ← gadget #2 │</span></span>
<span class="line"><span>├──────────────────────────────────────────┤</span></span>
<span class="line"><span>│  0                        ←  弹给 rsi</span></span>
<span class="line"><span>├──────────────────────────────────────────┤</span></span>
<span class="line"><span>│  0                        ←  弹给 rdx</span></span>
<span class="line"><span>├──────────────────────────────────────────┤</span></span>
<span class="line"><span>│  &amp;(pop rax ; ret)         ←  gadget #3</span></span>
<span class="line"><span>├──────────────────────────────────────────┤</span></span>
<span class="line"><span>│  59                       ←  弹给 rax</span></span>
<span class="line"><span>├──────────────────────────────────────────┤</span></span>
<span class="line"><span>│  &amp;(syscall ; ret)         ←  gadget #4 —— 触发 execve</span></span>
<span class="line"><span>└──────────────────────────────────────────┘</span></span>
<span class="line"><span></span></span>
<span class="line"><span>CPU 视角:</span></span>
<span class="line"><span>  原函数 ret → 跳到 gadget#1 → pop rdi(从栈弹&quot;/bin/sh&quot;地址) → ret</span></span>
<span class="line"><span>                            → 跳到 gadget#2 → pop rsi=0, pop rdx=0 → ret</span></span>
<span class="line"><span>                            → 跳到 gadget#3 → pop rax=59 → ret</span></span>
<span class="line"><span>                            → 跳到 gadget#4 → syscall  → 内核执行 execve → 拿到 /bin/sh</span></span></code></pre></div><p><strong>这一切发生在&quot;程序自己的代码段 + libc 代码段&quot;里,没有一个字节是攻击者注入的可执行代码</strong>——DEP / NX 完全没拦住,因为<strong>没人执行栈上的数据,执行的全是合法代码段里的指令</strong>。</p><h3 id="_4-3-rop-是图灵完备的" tabindex="-1">4.3 ROP 是图灵完备的 <a class="header-anchor" href="#_4-3-rop-是图灵完备的" aria-label="Permalink to &quot;4.3 ROP 是图灵完备的&quot;">​</a></h3><p>Shacham 2007 论文的核心贡献:<strong>只要程序里 gadget 足够多(libc 这种大库够用了),ROP 就是图灵完备的</strong> —— 能写循环、条件分支、内存读写,<strong>理论上可以用 gadget 拼出任意复杂的逻辑</strong>,不只是&quot;调 execve&quot;这种简单事。</p><p><strong>这意味着</strong>:NX/DEP 没有终结攻击,它<strong>只是把攻击者从「写新代码」逼到了「用旧代码拼新逻辑」</strong>。攻击门槛升高了一档,但攻击能力没有本质削弱。</p><h3 id="_4-4-gadget-怎么找-ropgadget-ropper" tabindex="-1">4.4 gadget 怎么找:ROPgadget / ropper <a class="header-anchor" href="#_4-4-gadget-怎么找-ropgadget-ropper" aria-label="Permalink to &quot;4.4 gadget 怎么找:ROPgadget / ropper&quot;">​</a></h3><p>人工从二进制里扒 gadget 是噩梦,<strong>有现成工具</strong>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ROPgadget</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --binary</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /lib/x86_64-linux-gnu/libc.so.6</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;pop rdi&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 0x000000000002a3e5 : pop rdi ; ret</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 0x000000000010f75b : pop rdi ; pop rbp ; ret</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ...几百条</span></span></code></pre></div><p><strong>ropper</strong> 是另一个同类工具,功能类似。<strong>libc 这种 2MB+ 的二进制里,通常能挖到几万个 gadget,足够拼任何逻辑</strong>。</p><p><strong>有趣的细节</strong>:gadget 不一定是程序&quot;原本写出来的指令&quot;,<strong>它可以是&quot;任意字节序列被反汇编出来的副产物&quot;</strong>。x86 是变长指令集,<strong>从一条指令的中间字节开始反汇编,得到的指令完全不同</strong>。攻击者会从这些&quot;对齐错位&quot;的字节里挖宝。这个 trick 在 RISC 架构(ARM、RISC-V)上不存在(指令定长),所以 ROP 在 ARM 上稍微难一点点,但依然能做。</p><hr><h2 id="五、rop-的衍生-jop-cop-srop" tabindex="-1">五、ROP 的衍生:JOP / COP / SROP <a class="header-anchor" href="#五、rop-的衍生-jop-cop-srop" aria-label="Permalink to &quot;五、ROP 的衍生:JOP / COP / SROP&quot;">​</a></h2><p>ROP 一火,学术界立刻把&quot;用别人代码片段拼逻辑&quot;这个思路推广到各种变种:</p><h3 id="_5-1-jop-jump-oriented-programming" tabindex="-1">5.1 JOP(Jump-Oriented Programming) <a class="header-anchor" href="#_5-1-jop-jump-oriented-programming" aria-label="Permalink to &quot;5.1 JOP(Jump-Oriented Programming)&quot;">​</a></h3><p><strong>用 <code>jmp rax</code> / <code>jmp [rax+8]</code> 这种间接跳转代替 <code>ret</code></strong>——目的是绕过那些&quot;专门检测 ret 异常&quot;的防御(早期 ROP 防御只盯着 ret)。<strong>Dispatcher gadget</strong> 负责调度,<strong>Functional gadget</strong> 负责干活。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ROP:   ret-driven —— 利用栈</span></span>
<span class="line"><span>JOP:   jmp-driven —— 利用某个寄存器当&quot;虚拟 PC&quot;</span></span></code></pre></div><h3 id="_5-2-cop-call-oriented-programming" tabindex="-1">5.2 COP(Call-Oriented Programming) <a class="header-anchor" href="#_5-2-cop-call-oriented-programming" aria-label="Permalink to &quot;5.2 COP(Call-Oriented Programming)&quot;">​</a></h3><p><strong>用 <code>call</code> 指令而不是 <code>jmp</code> / <code>ret</code></strong>——同样为了绕过&quot;ret-only&quot;的检测。</p><h3 id="_5-3-srop-sigreturn-oriented-programming" tabindex="-1">5.3 SROP(Sigreturn-Oriented Programming) <a class="header-anchor" href="#_5-3-srop-sigreturn-oriented-programming" aria-label="Permalink to &quot;5.3 SROP(Sigreturn-Oriented Programming)&quot;">​</a></h3><p><strong>最优雅的一种</strong>——利用 Linux 的 <code>sigreturn</code> 系统调用。Linux 处理信号时会把<strong>全部寄存器状态压栈</strong>,信号处理结束后用 <code>sigreturn</code> 一次性恢复。<strong>攻击者只要伪造一个&quot;signal frame&quot;压在栈上,然后跳到 sigreturn,内核就帮你一次性设置所有寄存器</strong>。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>普通 ROP:  要几十个 gadget 才能把 rdi/rsi/rdx/rax 都凑齐</span></span>
<span class="line"><span>SROP:      只要 1 个 sigreturn gadget + 一个伪造的 signal frame</span></span>
<span class="line"><span>           内核帮你一次性 set 所有寄存器</span></span></code></pre></div><p>SROP 在 2014 年由 Erik Bosman 提出,<strong>论文标题就叫「Framing Signals - A Return to Portable Shellcode」</strong>——把这种&quot;借力打力&quot;的美学发挥到了极致。</p><hr><h2 id="六、现代缓解-20-篇会全部细讲-这里先建立心智" tabindex="-1">六、现代缓解(20 篇会全部细讲,这里先建立心智) <a class="header-anchor" href="#六、现代缓解-20-篇会全部细讲-这里先建立心智" aria-label="Permalink to &quot;六、现代缓解(20 篇会全部细讲,这里先建立心智)&quot;">​</a></h2><h3 id="_6-1-aslr-address-space-layout-randomization" tabindex="-1">6.1 ASLR(Address Space Layout Randomization) <a class="header-anchor" href="#_6-1-aslr-address-space-layout-randomization" aria-label="Permalink to &quot;6.1 ASLR(Address Space Layout Randomization)&quot;">​</a></h3><p><strong>栈、堆、libc、可执行文件每次加载到随机地址</strong>——攻击者算 gadget 地址时,<strong>不知道 libc 这次加载到哪</strong>。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>不开 ASLR:                    开 ASLR:</span></span>
<span class="line"><span>libc 永远在 0x7ffff7a00000   libc 这次在 0x7f3a52400000</span></span>
<span class="line"><span>gadget 地址固定              下次在 0x7f8c31200000</span></span>
<span class="line"><span>exploit 一次写好永远能用       每次都得&quot;先泄露 libc 地址&quot;</span></span></code></pre></div><p><strong>绕过姿势</strong>:<strong>信息泄露(infoleak)</strong> —— 让程序漏一个指针出来,攻击者据此算出 libc 基址。<strong>90% 的现代漏洞利用都是&quot;infoleak + ROP&quot;两步走</strong>。</p><h3 id="_6-2-stack-canary" tabindex="-1">6.2 Stack Canary <a class="header-anchor" href="#_6-2-stack-canary" aria-label="Permalink to &quot;6.2 Stack Canary&quot;">​</a></h3><p><strong>编译器在每个函数的栈帧里、buf 和返回地址之间,塞一个随机值(canary,金丝雀)</strong>——函数返回前检查这个值是否被改动,<strong>改动了就 abort</strong>。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>栈帧布局(开了 Canary):</span></span>
<span class="line"><span>┌─────────────────┐</span></span>
<span class="line"><span>│  返回地址        │</span></span>
<span class="line"><span>├─────────────────┤</span></span>
<span class="line"><span>│  保存的 rbp     │</span></span>
<span class="line"><span>├─────────────────┤</span></span>
<span class="line"><span>│  CANARY 0x...   │  ← 函数 prologue 设,epilogue 查,变了就死</span></span>
<span class="line"><span>├─────────────────┤</span></span>
<span class="line"><span>│  buf[64]        │</span></span>
<span class="line"><span>└─────────────────┘</span></span>
<span class="line"><span></span></span>
<span class="line"><span>strcpy 溢出 buf:</span></span>
<span class="line"><span>   要覆盖返回地址 → 必须先覆盖 Canary → 检查不过 → __stack_chk_fail → 进程死</span></span></code></pre></div><p><strong>绕过姿势</strong>:<strong>泄露 Canary 值</strong> —— 一些格式化字符串漏洞 / 信息泄露原语能读出 Canary,然后写回原值再溢。<strong>fork 子进程的 Canary 和父进程一样</strong>,可以暴力爆破(每次错一位试)。</p><h3 id="_6-4-cfi-control-flow-integrity" tabindex="-1">6.4 CFI(Control Flow Integrity) <a class="header-anchor" href="#_6-4-cfi-control-flow-integrity" aria-label="Permalink to &quot;6.4 CFI(Control Flow Integrity)&quot;">​</a></h3><p><strong>编译器记录&quot;哪些跳转目标是合法的&quot;</strong>——每次间接调用 / 返回前,检查目标是否在白名单。理想中能拦死 ROP/JOP,<strong>实际部署因性能 / 兼容性,主流 Linux 还在推进</strong>。Windows 上有 CFG(Control Flow Guard)、Intel 有 CET。</p><h3 id="_6-5-shadow-stack-影子栈" tabindex="-1">6.5 Shadow Stack(影子栈) <a class="header-anchor" href="#_6-5-shadow-stack-影子栈" aria-label="Permalink to &quot;6.5 Shadow Stack(影子栈)&quot;">​</a></h3><p><strong>每个线程额外有一个&quot;只读&quot;的影子栈,专门只记返回地址</strong>。<code>call</code> 时正常栈 + 影子栈都压;<code>ret</code> 时比较两者,不一致就死。</p><p><strong>Intel CET</strong> 在硬件层做这事(Tiger Lake 之后的 CPU)、glibc 2.35+ 支持。<strong>这是栈溢出的&quot;终局解药&quot;</strong> —— 攻击者覆盖正常栈上的返回地址,影子栈不变,ret 时比较失败立刻 abort。</p><blockquote><p>ASLR / Canary / NX / CFI / Shadow Stack 这五件套构成「现代二进制缓解栈」—— <strong>每一道都能被单独绕,但叠在一起让 exploit 开发成本指数上升</strong>。详见 19 篇。</p></blockquote><hr><h2 id="七、真实事故-为什么内存安全语言-赢了" tabindex="-1">七、真实事故 &amp; 为什么内存安全语言&quot;赢了&quot; <a class="header-anchor" href="#七、真实事故-为什么内存安全语言-赢了" aria-label="Permalink to &quot;七、真实事故 &amp; 为什么内存安全语言&quot;赢了&quot;&quot;">​</a></h2><h3 id="_7-1-真实-cve-一瞥" tabindex="-1">7.1 真实 CVE 一瞥 <a class="header-anchor" href="#_7-1-真实-cve-一瞥" aria-label="Permalink to &quot;7.1 真实 CVE 一瞥&quot;">​</a></h3><ul><li><strong>CVE-2014-0160 Heartbleed(OpenSSL)</strong>:严格说是堆越界<strong>读</strong>,不是栈溢出,但<strong>同源问题</strong>——C 没边界检查。一行 <code>memcpy(bp, pl, payload);</code> 让全世界的 HTTPS 服务器漏私钥。</li><li><strong>CVE-2015-0235 GHOST(glibc gethostbyname)</strong>:<code>__nss_hostname_digits_dots</code> 函数里的栈溢出,几乎所有 Linux 系统受影响。</li><li><strong>CVE-2019-11932(WhatsApp GIF 解析)</strong>:Android 上的栈溢出,<strong>只要收到一张恶意 GIF,WhatsApp 进程被劫持</strong>——一次&quot;零点击&quot;攻击。</li><li><strong>CVE-2021-3156 Sudoedit Heap Overflow</strong>:Linux sudo 的堆溢出,十年老 bug,任意用户提权到 root。</li></ul><p><strong>共性</strong>:<strong>全是 C 写的核心基础设施 + 边界检查疏漏</strong>。从 1988 到 2024,<strong>根因没变过</strong>。</p><h3 id="_7-2-ctf-pwn-题-学栈溢出的合法操场" tabindex="-1">7.2 CTF Pwn 题:学栈溢出的合法操场 <a class="header-anchor" href="#_7-2-ctf-pwn-题-学栈溢出的合法操场" aria-label="Permalink to &quot;7.2 CTF Pwn 题:学栈溢出的合法操场&quot;">​</a></h3><p><strong>CTF(Capture The Flag)</strong> 是合法练习栈溢出 / ROP 的正确场景:</p><ul><li><strong>PicoCTF / PWN College / HackTheBox / TryHackMe</strong>:有专门的 Pwn 分类,<strong>自己搭好的靶机</strong></li><li><strong>典型题目难度阶梯</strong>:<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ret2win       —— 覆盖返回地址跳到一个现成的 win 函数(第二节那种)</span></span>
<span class="line"><span>ret2shellcode —— 关 NX 时,经典栈上 shellcode</span></span>
<span class="line"><span>ret2libc      —— 开 NX 但不开 ASLR,跳到 libc 的 system()</span></span>
<span class="line"><span>ROP           —— 开 NX + 部分 ASLR,自己拼 gadget</span></span>
<span class="line"><span>ROP + leak    —— 开全部缓解,先 leak libc 再 ROP</span></span></code></pre></div></li></ul><blockquote><p><strong>再次强调</strong>:<strong>这些技巧只在你自己控制的环境里练</strong>。对线上系统、别人的服务器跑这些,在中国是《刑法》285 条「非法侵入计算机信息系统罪」,在美国是 CFAA,<strong>轻则民事赔偿,重则刑事入狱</strong>。技术中立,行为不中立。</p></blockquote><h3 id="_7-3-为什么-rust-go-java-几乎免疫" tabindex="-1">7.3 为什么 Rust / Go / Java 几乎免疫 <a class="header-anchor" href="#_7-3-为什么-rust-go-java-几乎免疫" aria-label="Permalink to &quot;7.3 为什么 Rust / Go / Java 几乎免疫&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>┌──────────┬─────────────────────────────────────────────┐</span></span>
<span class="line"><span>│  语言    │  内存安全机制                                 │</span></span>
<span class="line"><span>├──────────┼─────────────────────────────────────────────┤</span></span>
<span class="line"><span>│  C/C++   │  程序员手动管理 —— 默认不安全                  │</span></span>
<span class="line"><span>│  Rust    │  借用检查器 + 所有权(编译期保证,零开销)      │</span></span>
<span class="line"><span>│  Go      │  GC + 运行时数组越界检查(panic)              │</span></span>
<span class="line"><span>│  Java    │  JVM + GC + 数组越界检查(ArrayIndexOOB)      │</span></span>
<span class="line"><span>│  Python  │  解释器 + 引用计数 + GC                       │</span></span>
<span class="line"><span>│  Swift   │  ARC + 严格边界检查                            │</span></span>
<span class="line"><span>└──────────┴─────────────────────────────────────────────┘</span></span></code></pre></div><p><strong>核心差异</strong>:<strong>这些语言访问数组时,运行时(或编译期)会检查 <code>index &lt; length</code></strong>——越界直接 panic / exception / abort,<strong>根本写不进相邻内存</strong>。<strong>没有越界,就没有栈溢出</strong>。</p><p><strong>代价</strong>:Java / Go 多一次比较 + 异常路径,<strong>性能损失约 5-10%</strong>。Rust 在编译期就证明了不越界,<strong>零运行时开销</strong>——这就是为什么 Rust 在系统编程领域势不可挡。</p><p><strong>业界已经在用脚投票</strong>:</p><ul><li><strong>Linux 内核</strong>:2022 年开始接受 Rust 代码(Rust for Linux)</li><li><strong>Microsoft</strong>:Windows 部分组件用 Rust 重写(2023)</li><li><strong>Google</strong>:Android 13 起,新代码 Rust 占比超过 C/C++,<strong>2024 内存安全 bug 数量大幅下降</strong></li><li><strong>AWS Firecracker</strong> / <strong>Cloudflare workerd</strong> / <strong>Discord</strong> —— 性能敏感的核心组件全在重写成 Rust</li></ul><blockquote><p><strong>从 1988 Morris 蠕虫到 2024 年,业界终于承认</strong>:<strong>&quot;教育程序员小心&quot;是个三十年都没解决的方案,换语言才是终局</strong>。详见 20 篇「内存安全语言为什么赢了」。</p></blockquote><hr><h2 id="八、踩坑提醒" tabindex="-1">八、踩坑提醒 <a class="header-anchor" href="#八、踩坑提醒" aria-label="Permalink to &quot;八、踩坑提醒&quot;">​</a></h2><ol><li><strong>以为「我用的是 strncpy 就安全了」</strong>——<code>strncpy(dst, src, n)</code> 如果 src 长度 ≥ n,<strong>dst 末尾不会有 <code>\\0</code></strong>,后续 strlen / strcpy 接力会越界。用 <code>strlcpy</code> 或 <code>snprintf</code>。</li><li><strong>以为 Canary 拦死所有栈溢出</strong>——能泄露 Canary 的话(fork、格式化字符串、读越界)就能绕过。</li><li><strong>以为 ASLR 一开就万事大吉</strong>——只随机化基址,模块内偏移不变;只要一个 infoleak 就重新可控。<strong>32 位系统熵不够,几小时能爆破完</strong>。</li><li><strong>以为关掉栈可执行就没事</strong>——ROP / ret2libc 不需要可执行栈。</li><li><strong>以为「我代码没调 strcpy」就安全</strong>——你依赖的库可能调,堆上的对象可能有指针,<strong>整个进程的安全是木桶</strong>。</li><li><strong>以为 ROP 只能在 x86 上</strong>——ARM、RISC-V、MIPS 全部可以 ROP,只是 gadget 风格不同。手机、IoT 设备同样是攻击面。</li><li><strong>CTF 学到的 exploit 别拿到外网用</strong>——靶机是教学,<strong>线上是犯罪</strong>。一次冲动毁掉整个职业生涯,<strong>已经有大量真实案例</strong>。</li><li><strong>以为 Rust = 绝对安全</strong>——Rust 有 <code>unsafe</code> 块、有 FFI 调 C 库、有逻辑漏洞,<strong>只是把&quot;内存安全&quot;这一类问题消灭了</strong>,其他漏洞依然存在。</li><li><strong>以为旧 C 代码&quot;重要但稳定就别动&quot;</strong>——OpenSSL / Linux 内核每年都还在出栈溢出 CVE,<strong>&quot;稳定&quot;的代码不等于&quot;安全&quot;的代码</strong>。</li><li><strong>以为这一章是&quot;历史课&quot;</strong>——2024 年的 CVE-2024-0193(Linux netfilter UAF)、CVE-2024-1086 等,<strong>内存安全漏洞依然在大量产出</strong>,只是攻击者再也不能&quot;一行 payload 一台机器&quot;了。</li></ol><hr><h2 id="九、本篇与下一篇" tabindex="-1">九、本篇与下一篇 <a class="header-anchor" href="#九、本篇与下一篇" aria-label="Permalink to &quot;九、本篇与下一篇&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>16 程序在内存里长什么样 ✓  ← 栈帧 / 返回地址的物理基础</span></span>
<span class="line"><span>17 栈溢出与 ROP (本篇)     ← 攻击姿势的两代演化</span></span>
<span class="line"><span>18 堆漏洞                  ← UAF / Double Free / Heap Spray 是另一套话题</span></span>
<span class="line"><span>19 内存安全缓解机制         ← ASLR / Canary / CFI / Shadow Stack 深入</span></span>
<span class="line"><span>20 模糊测试与内存安全语言    ← AFL / libFuzzer / 为什么 Rust 必赢</span></span></code></pre></div><p>下一篇 <code>18-堆漏洞.md</code>:讲清楚 <strong>glibc malloc 的 bin 结构、为什么 double free 能让攻击者&quot;凭空&quot;得到一块&quot;被别人也持有的内存&quot;、use-after-free 在 C++ 虚函数表场景下怎么被劫持成任意代码执行、heap spraying 在浏览器漏洞里怎么&quot;用大量喷洒抵消 ASLR 的随机性&quot;</strong>。栈漏洞需要&quot;控制返回地址&quot;,堆漏洞需要&quot;控制元数据&quot;——攻击者视角完全不同,但<strong>根因依然是 C 不做边界检查 + 释放后内存复用</strong>。</p><hr><p><strong>最后一句送给读完这篇的你</strong>:<strong>学栈溢出不是为了去打别人的服务器,是为了在自己写 / 评审 C/C++ 代码时,看到 <code>strcpy(dst, src)</code> 这一行就本能地浑身一紧</strong>——这种&quot;被攻击的本能&quot;才是这一系列要给你的东西。<strong>那种本能值钱</strong>。</p>`,130)])])}const u=a(i,[["render",e]]);export{d as __pageData,u as default};
